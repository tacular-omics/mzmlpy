{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"mzmlpy","text":"<p>A lightweight Python library for parsing mzML mass spectrometry files. Initially built from pymzml, it implements a more straightforward, type-safe API and includes direct support for modern mzML structures (&gt; 1.1.0).</p> <p> </p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install mzmlpy\n</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML.gz\") as reader:\n    print(f\"File ID: {reader.id}\")\n    print(f\"Total Spectra: {len(reader.spectra)}\")\n\n    for spectrum in reader.spectra:\n        print(f\"Scan {spectrum.id} (MS{spectrum.ms_level}) - TIC: {spectrum.TIC}\")\n</code></pre> <p>The <code>Mzml</code> reader lazily loads data, meaning binary arrays and metadata are only parsed when you access them. It supports <code>.mzML</code> and <code>.mzML.gz</code> files, and exposes spectra and chromatograms via lookup objects that support iteration, integer indexing, slicing, and string-based ID lookup.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Lazy parsing -- binary data is decoded only when accessed.</li> <li>Type-safe API -- dataclass-based models with full type annotations.</li> <li>Flexible access -- look up spectra and chromatograms by index, slice, or string ID.</li> <li>Ion mobility support -- detect and retrieve IM data, whether stored as a binary array or a scan-level cvParam (e.g. Bruker timsTOF PASEF MS2).</li> <li>Comprehensive compression -- zlib, zstd, and MS-Numpress decoders built in.</li> <li>Fast -- 5--9x faster than pymzml on complex files, ~1.2x on small files.</li> <li>Context manager -- use <code>with</code> for safe file handling.</li> <li>Actionable errors -- decode failures name the offending value and, for optional dependencies, the extra to install.</li> </ul>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Getting Started -- installation details, basic usage patterns, and working with binary data.</li> <li>Benchmarks -- performance comparison against pymzml and gzip mode timings.</li> <li>API Reference -- full auto-generated documentation for every public class and method.</li> </ul> <p>Using an AI coding assistant? Point it at <code>llms.txt</code> in the repository root for a compact, accurate API guide.</p>"},{"location":"benchmarks/","title":"Benchmarks","text":""},{"location":"benchmarks/#mzmlpy-vs-pymzml","title":"mzmlpy vs pymzml","text":"<p>Comparison of mzmlpy and pymzml 2.6.0 across common mzML parsing operations. Both libraries use default settings: pymzml with <code>build_index_from_scratch=False</code> and <code>skip_chromatogram=True</code>; mzmlpy with default constructor arguments. A warmup phase primes both libraries before timing so that one-time costs (OBO ontology loading, OS page cache) don't skew results.</p>"},{"location":"benchmarks/#methodology","title":"Methodology","text":"<p>Each benchmark is run 10 times. The table shows mean \u00b1 standard deviation. Both libraries are verified to produce identical m/z and intensity arrays before timing begins.</p> <ul> <li>Startup \u2014 open the file and build the spectrum index.</li> <li>Iterate (no decode) \u2014 iterate all spectra, accessing only <code>id</code> and <code>ms_level</code>.</li> <li>Iterate (decode) \u2014 iterate all spectra, decoding m/z and intensity arrays.</li> <li>Metadata \u2014 iterate all spectra, accessing scan time, TIC, and precursor info.</li> <li>Random access \u2014 seek to 10 random spectra by index/ID and decode arrays.</li> </ul>"},{"location":"benchmarks/#zlib-compressed-dda-file-10-spectra-527-kb","title":"zlib-compressed DDA file (10 spectra, 527 KB)","text":"Benchmark mzmlpy pymzml Ratio Startup 0.0017s 0.0021s 1.2x faster Iterate (no decode) 0.0059s 0.0074s 1.3x faster Iterate (decode) 0.0071s 0.0087s 1.2x faster Metadata 0.0069s 0.0077s 1.1x faster Random access 0.0047s 0.0053s 1.1x faster"},{"location":"benchmarks/#bruker-timstof-file-with-ion-mobility-10-spectra-67-mb","title":"Bruker timsTOF file with ion mobility (10 spectra, 6.7 MB)","text":"Benchmark mzmlpy pymzml Ratio Startup 0.012s 0.092s 8.0x faster Iterate (no decode) 0.040s 0.221s 5.5x faster Iterate (decode) 0.039s 0.228s 5.8x faster Metadata 0.042s 0.226s 5.4x faster Random access 0.012s 0.110s 9.2x faster <p>The gap widens on larger, more complex files. The Bruker file contains ion mobility data and richer XML metadata, where mzmlpy's parser is 5--9x faster.</p>"},{"location":"benchmarks/#running-the-benchmark","title":"Running the benchmark","text":"<pre><code>pip install pymzml  # required dependency for comparison\n\n# Default file\nuv run python benchmarks/bench_vs_pymzml.py\n\n# Custom file with more repeats\nuv run python benchmarks/bench_vs_pymzml.py --file path/to/file.mzML --repeats 10\n</code></pre> <p>See <code>benchmarks/bench_vs_pymzml.py</code> for the full source.</p>"},{"location":"benchmarks/#gzip-mode-comparison","title":"Gzip mode comparison","text":"<p>For <code>.mzML.gz</code> files, the <code>gzip_mode</code> parameter controls how the compressed file is accessed. Benchmarked on a 33,535-spectrum DDA file (cold start, with rapidgzip):</p> Mode Startup Iterate (500 spectra) Random access (5 reads) plain <code>.mzML</code> 0.042s 0.087s 0.001s <code>in_memory=True</code> 1.499s 0.362s 0.002s <code>gzip_mode=\"extract\"</code> 0.957s 0.083s 0.001s <code>gzip_mode=\"indexed\"</code> 6.850s 0.135s 0.074s <code>gzip_mode=\"stream\"</code> 0.089s 0.155s 22.8s <p><code>\"extract\"</code> pays a one-time decompression cost then matches plain <code>.mzML</code> speed. <code>\"indexed\"</code> startup includes building the gzip seek index and mzML offset index on first open \u2014 both are cached alongside the file, so subsequent opens are fast. <code>\"stream\"</code> is sequential-only \u2014 random access requires re-scanning from the start.</p> <p>See <code>benchmarks/bench_gzip_modes.py</code> for the full source.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<p>Install from PyPI:</p> <pre><code>pip install mzmlpy\n</code></pre> <p>If you need MS-Numpress decoding support, install the optional extra:</p> <pre><code>pip install mzmlpy[numpress]\n</code></pre>"},{"location":"getting-started/#basic-usage","title":"Basic Usage","text":"<p>Open an mzML file with the context manager to ensure proper cleanup:</p> <pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    print(f\"File ID: {reader.id}\")\n    print(f\"mzML version: {reader.version}\")\n</code></pre> <p>Both <code>.mzML</code> and <code>.mzML.gz</code> files are supported. The reader lazily parses the file, so metadata is available immediately while binary data is decoded only on access.</p>"},{"location":"getting-started/#reading-gzipped-files","title":"Reading Gzipped Files","text":"<p>When working with <code>.mzML.gz</code> files, the <code>gzip_mode</code> parameter controls how the compressed file is accessed:</p> <p><code>gzip_mode=\"auto\"</code> is the default. With <code>in_memory=False</code>, it selects an embedded index, a current extracted cache, or complete rapidgzip sidecars in that order. If none exists, it creates an extracted cache. Inspect <code>reader.access_strategy</code> to see the concrete route.</p> <p>For fast random access without cache files, create a self-indexed gzip file once:</p> <pre><code>from pathlib import Path\nfrom tempfile import TemporaryDirectory\n\nfrom mzmlpy import Mzml, write_indexed_gzip\n\nwith TemporaryDirectory() as directory:\n    output = Path(directory) / \"input.indexed.mzML.gz\"\n    write_indexed_gzip(\"tests/data/example.mzML\", output)\n\n    with Mzml(output, in_memory=False) as reader:\n        spectrum = reader.spectra[0]\n</code></pre> <p>mzMLPy detects this pyMZML-compatible embedded format automatically. The file remains a standard concatenated gzip stream, and decompressing it reconstructs the original mzML bytes exactly.</p> <ul> <li><code>\"auto\"</code> (default) selects the best valid representation already available and otherwise extracts into the central cache.</li> <li><code>\"extract\"</code> decompresses to a cached file under the OS temp directory (<code>&lt;tmpdir&gt;/mzmlpy/</code>), then reads with full random access. The cache persists across Python sessions so subsequent opens of the same file skip decompression entirely. The OS clears the temp directory on reboot. Call <code>clear_cache()</code> to reclaim space sooner.</li> <li><code>\"indexed\"</code> \u2014 Use the <code>rapidgzip</code> library for seekable access to the compressed file without extracting to disk. Requires <code>pip install mzmlpy[rapidgzip]</code>. Builds a gzip seek index (<code>.gzidx</code>) and mzML offset index (<code>.mzMLidx</code>) on first open, cached alongside the file for instant startup on subsequent opens.</li> <li><code>\"stream\"</code> \u2014 Stream the file sequentially with no index. Lowest startup cost, but random access (e.g. <code>reader.spectra[0]</code>) scans from the beginning each time \u2014 a warning is emitted.</li> </ul> <p><code>\"extract\"</code> pays a one-time decompression cost then matches plain <code>.mzML</code> speed on later opens of the same file (the extracted copy is cached). <code>\"indexed\"</code> pays a one-time index-build cost for seekable access with no disk copy, then fast random access on later opens (the index is cached alongside the file). <code>\"stream\"</code> has the lowest startup cost, but random access re-scans from the start each time. For a reproducible benchmark with real numbers \u2014 including a comparison against pyteomics and pymzml \u2014 see <code>benchmarks/</code> in the repository.</p> <p>For best performance with <code>.mzML.gz</code> files, use <code>\"extract\"</code> or <code>\"indexed\"</code>:</p> <pre><code>from mzmlpy import Mzml\n\n# Indexed mode \u2014 no extraction, seekable (requires rapidgzip)\nwith Mzml(\"tests/data/example.mzML.gz\", gzip_mode=\"indexed\", in_memory=False) as reader:\n    print(f\"Spectra: {len(reader.spectra)}\")\n    spec = reader.spectra[0]\n    print(spec.id)\n</code></pre> <p>To reclaim disk space before the OS clears the temp directory on reboot:</p> <pre><code>from mzmlpy import clear_cache\nclear_cache()\n</code></pre>"},{"location":"getting-started/#iterating-spectra","title":"Iterating Spectra","text":"<p>The <code>reader.spectra</code> property returns a lookup object that supports iteration, integer indexing, slicing, and string ID lookup:</p> <pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    # Iterate all spectra\n    for spectrum in reader.spectra:\n        print(f\"Scan {spectrum.id} (MS{spectrum.ms_level}) - TIC: {spectrum.TIC}\")\n\n    # Access by index\n    first = reader.spectra[0]\n\n    # Access by slice\n    batch = reader.spectra[0:2]\n\n    # Access by string ID\n    scan = reader.spectra[\"scan=19\"]\n\n    # Filter with a list comprehension\n    ms2_spectra = [s for s in reader.spectra if s.ms_level == 2]\n</code></pre>"},{"location":"getting-started/#native-ids-and-summary-values","title":"Native IDs and Summary Values","text":"<p>The native <code>id</code> string encodes vendor-specific components (e.g. Thermo's <code>controllerType=0 controllerNumber=1 scan=19</code>); <code>id_dict</code> parses it into a dict with numeric components coerced to <code>int</code>. Common summary values and the instrument scan filter are also exposed directly, instead of requiring a manual <code>get_cvparm</code> lookup:</p> <pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    spec = reader.spectra[0]\n\n    print(spec.id_dict)  # e.g. {\"scan\": 19}\n\n    print(spec.base_peak_mz, spec.base_peak_intensity)\n    print(spec.lowest_observed_mz, spec.highest_observed_mz)\n    print(spec.filter_string)  # e.g. Thermo scan filter string\n</code></pre> <p>To cheaply check how many spectra a file contains without opening it fully -- no reader is constructed and no random-access index is built, so this is much cheaper than <code>len(Mzml(path).spectra)</code> when you only need the count -- use the standalone <code>peek_spectrum_count</code> function:</p> <pre><code>from mzmlpy import peek_spectrum_count\n\ncount = peek_spectrum_count(\"tests/data/example.mzML\")  # int | None\n</code></pre>"},{"location":"getting-started/#accessing-binary-data","title":"Accessing Binary Data","text":"<p>Spectra expose <code>mz</code> and <code>intensity</code> as convenience properties. Access is lazy -- the binary data is decoded on every call, so save the result to a local variable when you need it more than once:</p> <pre><code>from mzmlpy import Mzml\nfrom mzmlpy import constants as c\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    spec = reader.spectra[0]\n\n    mz = spec.mz  # np.ndarray | None\n    intensity = spec.intensity  # np.ndarray | None\n    charge = spec.charge  # np.ndarray | None\n\n    # For less common array types, use get_binary_array with a CV accession\n    barr = spec.get_binary_array(c.BinaryDataArrayAccession.RAW_ION_MOBILITY)\n    if barr is not None:\n        values = barr.data\n\n    # Iterate all binary arrays on a spectrum\n    for ba in spec.binary_arrays:\n        print(ba.binary_array_type, ba.compression, ba.encoding)\n</code></pre>"},{"location":"getting-started/#working-with-scan-timing","title":"Working with Scan Timing","text":"<p>Retention time and ion injection time are accessible as <code>timedelta</code> objects through the spectrum, which delegates to the first scan:</p> <pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    spec = reader.spectra[0]\n\n    if spec.scan_start_time is not None:\n        rt_seconds = spec.scan_start_time.total_seconds()\n        rt_minutes = rt_seconds / 60\n        print(f\"RT: {rt_minutes:.4f} min\")\n\n    if spec.ion_injection_time is not None:\n        iit_ms = spec.ion_injection_time.total_seconds() * 1000\n        print(f\"Ion injection time: {iit_ms:.2f} ms\")\n\n    print(f\"Lower m/z: {spec.lower_mz}\")\n    print(f\"Upper m/z: {spec.upper_mz}\")\n</code></pre>"},{"location":"getting-started/#working-with-ion-mobility","title":"Working with Ion Mobility","text":"<p>Check whether a spectrum carries ion mobility data and retrieve the relevant arrays:</p> <pre><code>from mzmlpy import Mzml\nfrom mzmlpy.constants import BinaryDataArrayAccession\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    spec = reader.spectra[0]\n\n    if spec.has_im:\n        print(f\"IM types: {spec.im_types}\")\n\n        im_array = spec.get_binary_array(\n            BinaryDataArrayAccession.MEAN_INVERSE_REDUCED_ION_MOBILITY\n        )\n        if im_array is not None:\n            values = im_array.data\n</code></pre>"},{"location":"getting-started/#working-with-chromatograms","title":"Working with Chromatograms","text":"<p>Chromatograms work similarly to spectra -- access by index, ID, or iteration:</p> <pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    tic = reader.chromatograms[\"tic\"]\n\n    time = tic.time  # np.ndarray | None\n    intensity = tic.intensity  # np.ndarray | None\n\n    # Precursor and product info (SRM chromatograms)\n    print(tic.precursor)\n    print(tic.product)\n    print(tic.chromatogram_type)  # \"tic\", \"basepeak\", \"srm\", etc.\n</code></pre>"},{"location":"getting-started/#accessing-file-metadata","title":"Accessing File Metadata","text":"<p>The reader exposes instrument configuration, software, and other file-level metadata:</p> <pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\") as reader:\n    # Instrument configurations\n    for config_id, config in reader.instrument_configurations.items():\n        print(f\"Instrument: {config_id}\")\n        print(f\"  Sources: {len(config.source_components)}\")\n        print(f\"  Analyzers: {len(config.analyzer_components)}\")\n        print(f\"  Detectors: {len(config.detector_components)}\")\n\n    # Software\n    for sw in reader.softwares.values():\n        print(f\"{sw.id} v{sw.version}\")\n\n    # Other metadata\n    _ = reader.cvs\n    _ = reader.file_description\n    _ = reader.referenceable_param_groups\n    _ = reader.data_processes\n    _ = reader.samples\n    _ = reader.scan_settings\n    _ = reader.run\n</code></pre>"},{"location":"getting-started/#validation","title":"Validation","text":"<p>Use <code>validate</code> to check a file directly without creating or repairing caches. The default scans XML, checks list counts, duplicate IDs, references, index ID agreement, and supported array metadata. It does not decode binary arrays.</p> <pre><code>from mzmlpy import validate\n\nreport = validate(\"tests/data/example.mzML\")\nprint(report.valid, report.spectrum_count, report.chromatogram_count)\nfor issue in report.issues:\n    print(issue.code, issue.location, issue.message)\n</code></pre> <p>Set <code>decode_binary=True</code> to decode arrays and compare their lengths. Set <code>check_index=True</code> to seek to XML footer offsets and verify their targets. These checks may be expensive, especially offset verification on ordinary gzip files. The report states which checks ran, how many arrays and index entries were checked, and whether XML parsing completed. <code>report.to_dict()</code> returns JSON-serializable results. File-open errors raise <code>OSError</code>. Malformed content is reported through <code>report.issues</code>.</p> <p>An open reader also has <code>reader.validate(...)</code>. It uses a fresh handle to the selected representation and preserves the lookup cursor. Use standalone <code>validate(path)</code> when the original source file, rather than a cached representation, is what you want to inspect. These checks do not constitute full XSD or controlled-vocabulary validation, and they do not verify the embedded gzip index itself.</p>"},{"location":"getting-started/#numeric-types","title":"Numeric types","text":"<p>Decoded arrays now preserve the numeric type declared in the file: <code>float32</code>, <code>float64</code>, <code>int32</code>, or <code>int64</code>. This applies to spectrum, chromatogram, charge, and mobility arrays, including empty arrays. Arrays remain writable and each access decodes a fresh array.</p> <p>This changes the previous behavior, which converted ordinary arrays to float64. Preserving the stored type avoids rounding large integers and uses half the array memory for float32 and int32 data. Code that needs float64 for calculations can convert explicitly:</p> <pre><code>import numpy as np\nfrom mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\", in_memory=False) as reader:\n    spectrum = reader.spectra[0]\n    intensity = spectrum.intensity.astype(np.float64)\n</code></pre> <p>Choose calculation types deliberately. Arithmetic on integer arrays can overflow, and float32 arithmetic can round differently from float64. Existing consumers that require double precision should use the explicit conversion above.</p> <p>Numpress is a compressed numerical representation with its own reconstruction rules. Its decoded output remains float64, including empty arrays, without an extra narrowing cast. Decoding cannot recover precision discarded during lossy encoding. See the Numpress format description. For arrays without a declared numeric type, the existing warning and float64 fallback remain.</p>"},{"location":"getting-started/#lazy-filtering","title":"Lazy filtering","text":"<p><code>reader.spectra.filter(...)</code> selects spectra from metadata without decoding their binary arrays. All supplied criteria must match. Bounds are inclusive, and <code>None</code> leaves an endpoint open. Retention times are expressed in seconds, with source units normalized.</p> <pre><code>from mzmlpy import Mzml\n\nwith Mzml(\"tests/data/example.mzML\", in_memory=False) as reader:\n    selected = reader.spectra.filter(ms_level=2, retention_time=(0, None))\n    for spectrum in selected:\n        print(spectrum.id, spectrum.ms_level)\n</code></pre> <p>Available criteria are <code>ms_level</code>, <code>retention_time=(lower_seconds, upper_seconds)</code>, <code>polarity=\"positive\"</code> or <code>\"negative\"</code>, <code>precursor_mz=(lower_mz, upper_mz)</code>, <code>spectrum_type=\"centroid\"</code> or <code>\"profile\"</code>, and scan-level mobility or FAIMS selection. Retention time matches any scan. Precursor m/z matches overlap with any reported isolation window. Selected-ion m/z values are used when a precursor has no usable isolation window. Missing metadata does not match a requested criterion. Invalid numeric metadata raises its normal contextual error. <code>SpectrumFilter</code> provides the same reusable predicate through its <code>matches(spectrum)</code> method.</p> <p>For mobility selection, use <code>mobility_type=\"inverse_reduced\"</code> or <code>\"drift_time\"</code> and optionally <code>ion_mobility=(lower, upper)</code>. Bounds use the recorded scan quantity and require an explicit mobility type. <code>faims_voltage=(lower, upper)</code> accepts signed volts. These criteria inspect scan metadata and do not process per-peak mobility arrays.</p> <p>Filtering is a sequential scan. Keep the reader open while consuming the returned iterator. It neither builds a retention-time index nor changes the cursor used by <code>reader.spectra.next()</code>.</p>"},{"location":"getting-started/#command-line-inspection","title":"Command-line inspection","text":"<p>The CLI emits JSON and needs no additional installation:</p> <pre><code>python -m mzmlpy inspect data.mzML\npython -m mzmlpy validate data.mzML --decode-binary --check-index\npython -m mzmlpy index-gzip data.mzML data.indexed.mzML.gz\n</code></pre> <p>Exit codes are <code>0</code> for success, <code>1</code> for validation findings with errors, and <code>2</code> for an operational error. Inspection reads metadata and counts without decoding arrays.</p>"},{"location":"getting-started/#memory-and-extracted-caches","title":"Memory and extracted caches","text":"<p><code>in_memory=True</code> remains the reader default. Use <code>in_memory=False</code> for large files and to activate gzip access strategies. Extraction copies decompressed chunks directly to disk. Sequential iteration detaches completed spectra and chromatograms, including records that are skipped while finding the requested kind. Keeping returned spectra in a list still retains their XML in your own code.</p> <p>Both the default cache and a custom <code>extract_dir</code> use filenames based on source identity and filesystem revision. Files with matching basenames in different directories do not share an extracted file. Replacing a source creates a new cache path, so existing readers can continue using their previous extracted copy. Older cache files can remain until you clean the directory. <code>clear_cache()</code> removes the default cache only.</p>"},{"location":"mcp/","title":"MCP integration","text":"<p>The optional MCP server connects AI clients to local mzML files. Its scope is discovery, recorded metadata, validation, data selection, and export. Spectrum processing belongs to Spectacular. Plotting belongs to a visualization client.</p> <p>MCP integration is available starting with version 0.9.0:</p> <pre><code>pip install \"mzmlpy[mcp]\"\npython -m mzmlpy mcp --root /absolute/path/to/data\n</code></pre> <p>The base installation still requires only NumPy. Importing <code>mzmlpy</code> or its data-access helpers does not import the optional MCP SDK. Add codec extras when needed, for example <code>pip install \"mzmlpy[mcp,numpress,zstd]\"</code>.</p> <p>To enable explicit export requests, supply an existing output directory:</p> <pre><code>python -m mzmlpy mcp --root /absolute/path/to/data --output-dir /absolute/path/to/exports\n</code></pre> <p>The server exposes 17 tools by default and two additional export tools with <code>--output-dir</code>. It uses local stdio, with no network listener. Source files are never modified, and the server does not create extracted caches or sidecar indexes.</p>"},{"location":"mcp/#client-configuration","title":"Client configuration","text":"<p>Point a local MCP client at the Python interpreter where you installed the extra:</p> <pre><code>{\n  \"mcpServers\": {\n    \"mzmlpy\": {\n      \"command\": \"/absolute/path/to/venv/bin/python\",\n      \"args\": [\"-m\", \"mzmlpy\", \"mcp\", \"--root\", \"/absolute/path/to/data\"]\n    }\n  }\n}\n</code></pre> <p>On Windows, use the environment's <code>Scripts/python.exe</code>. Add <code>--output-dir</code> and its path to <code>args</code> to enable exports. The client controls how returned data is sent to a model provider. Diagnostics go to stderr, while stdout carries only the MCP protocol.</p>"},{"location":"mcp/#tools","title":"Tools","text":"Tool Purpose <code>server_info</code> Report capabilities, codec availability, operating limits, and scope <code>list_files</code> Page mzML files and subdirectories, with filename glob matching <code>inspect_file</code> File metadata, reader-reported counts, instrument terms, and software <code>get_metadata</code> Page header sections, preserving CV terms, user parameters, references, and timestamps <code>summarize_run</code> Inventory recorded acquisition metadata without decoding peaks <code>compare_runs</code> Compare metadata inventories and instruments for 2 through 8 files <code>validate_file</code> Structural validation, with optional binary and XML offset checks <code>find_spectra</code> Page spectra selected by metadata <code>get_spectrum</code> Exact native ID metadata and optional bounded peak pairs <code>get_spectra</code> Retrieve metadata for up to 20 exact IDs in one scan <code>list_chromatograms</code> Page stored chromatogram IDs and metadata without decoding <code>get_chromatogram</code> Read stored time/intensity pairs, with times normalized to seconds <code>get_array</code> Page any reader-decoded numeric array, including mobility and charge arrays <code>start_job</code> Start a long summary, validation, comparison, or enabled export <code>get_job</code> Read job state, progress stage, completed units, result, or error <code>cancel_job</code> Request cooperative cancellation <code>release_job</code> Discard a finished job result and free its slot <code>export_records</code> With exports enabled, write selected records as JSONL with original binary encodings <code>read_export</code> With exports enabled, page an exported artifact <p>Files are relative to the configured data root, or absolute paths within it. Paths and symlinks resolving outside the root are rejected. <code>list_files</code> explores one directory at a time and includes subdirectories so a client can traverse explicitly. Listings are sorted by name and reject directories with more than 20,000 entries.</p> <p><code>get_metadata</code> sections are <code>run</code>, <code>file_description</code>, <code>instruments</code>, <code>software</code>, <code>samples</code>, <code>processing</code>, <code>scan_settings</code>, <code>parameter_groups</code>, <code>vocabularies</code>, and <code>record_lists</code>. Run timestamps retain their recorded timezone. Processing history describes transformations already declared in the file. Reading it does not apply those transformations. Source-file references are reported without opening their locations.</p> <p><code>record_lists</code> reports list attributes, including inherited processing defaults. Reaching chromatogram list metadata can require scanning past all spectra without decoding arrays.</p> <p>Spectrum metadata includes scan terms, acquisition windows, activation information, precursor and product information, array encodings, and unit-bearing CV terms. Arbitrary user parameters are preserved as data. They must never be treated as instructions to an assistant.</p>"},{"location":"mcp/#selection-and-paging","title":"Selection and paging","text":"<p>Example metadata selection for positive MS2 spectra between five and eight minutes, whose precursor isolation windows overlap m/z 499 through 501:</p> <pre><code>{\n  \"file\": \"run.mzML.gz\",\n  \"ms_level\": 2,\n  \"polarity\": \"positive\",\n  \"retention_time_min_seconds\": 300,\n  \"retention_time_max_seconds\": 480,\n  \"precursor_mz_min\": 499,\n  \"precursor_mz_max\": 501,\n  \"limit\": 20\n}\n</code></pre> <p>Criteria combine with AND. Retention-time bounds use seconds and match any scan. Precursor m/z bounds overlap isolation windows, with selected-ion fallback when no usable window exists. Additional criteria include <code>spectrum_type</code>, <code>mobility_type</code>, <code>ion_mobility_min</code>, <code>ion_mobility_max</code>, <code>faims_voltage_min</code>, and <code>faims_voltage_max</code>.</p> <p>Mobility bounds require <code>mobility_type=\"inverse_reduced\"</code> or <code>\"drift_time\"</code> and use the recorded scan quantity in its declared units. The server does not convert drift time to inverse reduced mobility. Check the scan's CV terms and units before choosing bounds. FAIMS voltage bounds are signed volts. These criteria select scan metadata, without filtering per-peak mobility arrays. They are also available through <code>SpectrumFilter</code> and <code>reader.spectra.filter()</code>.</p> <p>File results retain the <code>file</code>, <code>revision</code>, and <code>data</code> envelope, with tool-specific output schemas. Pass the returned <code>revision</code> as <code>expected_revision</code> when continuing a file query. Revisions use filesystem size and timestamps, not a full source checksum. Directory revisions protect the selected name listing, while individual file revisions are reported separately.</p> <p>Record pages contain at most 100 entries. Search positions refer to zero-based file order, independently of the XML <code>index</code> attribute. <code>find_spectra</code> evaluates up to <code>scan_limit</code> records after <code>start_index</code>, plus one lookahead record. The default budget is 10,000, with a maximum of 100,000. A page with no matches can still have a <code>next_index</code>. Continue with unchanged filters until <code>exhausted</code> is true.</p> <p>Array and peak pages contain at most 1,000 values or pairs. Their positions refer to the original arrays, including when coordinate bounds exclude some points. A null <code>next_index</code> means no matching points remain. Arrays are returned in their recorded order without smoothing, normalization, peak picking, or downsampling.</p> <p><code>get_array</code> exposes values in original units and reports the decoded <code>dtype</code>. Ordinary arrays retain the stored numeric type. Numpress reconstructs float64 values. Nonfinite floats use explicit <code>NaN</code>, <code>Infinity</code>, and <code>-Infinity</code> strings.</p> <p>All array and paired-point tools return integers within <code>[-(2**53-1), 2**53-1]</code> as JSON integers. Larger integers use exact decimal strings to prevent rounding by clients that parse JSON numbers as doubles. Use the reported dtype to interpret these strings. Float32 values are represented by their exact numerical value as JSON floats, with dtype recorded separately because JSON has no float32 type.</p> <p>Paired points report <code>coordinate_dtype</code> and <code>intensity_dtype</code>. Chromatogram coordinates remain in seconds. Conversion from other time units uses float64, while <code>get_array</code> retains the original time values and type. The paired tools reject nonfinite coordinates or intensities and mismatched lengths. Encoded exports retain original binary representations.</p>"},{"location":"mcp/#inventories-and-comparisons","title":"Inventories and comparisons","text":"<p><code>summarize_run</code> scans recorded metadata and reports counts by MS level, polarity, spectrum representation, array type, and compression. It includes declared empty-array counts, missing retention times, recorded time ranges, multi-scan counts, and observed isolation windows. At most 100 distinct isolation windows are returned, with explicit truncation.</p> <p>Timing statistics use the first scan of each consecutive spectrum. Missing first-scan times break adjacency. The largest nonnegative gap and backward-time count are descriptive values, without an assumed threshold for an abnormal run. Summaries do not decode intensities or compute new chromatograms, and they do not establish scientific quality.</p> <p><code>compare_runs</code> reports exact differences between inventories and instrument metadata. It performs no spectrum matching, retention-time alignment, signal comparison, or equivalence testing. Matching metadata does not imply matching measurements.</p>"},{"location":"mcp/#long-operations","title":"Long operations","text":"<p>Start a background operation with its tool arguments:</p> <pre><code>{\n  \"operation\": \"summarize_run\",\n  \"arguments\": {\"file\": \"run.mzML.gz\"}\n}\n</code></pre> <p>Use the returned <code>job_id</code> with <code>get_job</code>. The response reports <code>queued</code>, <code>running</code>, <code>completed</code>, <code>failed</code>, or <code>cancelled</code>, plus the current stage and completed units. Stage counters describe records or XML-offset checks, not a percentage, and can reset when the stage changes.</p> <p>Two background workers share at most eight retained jobs. Completed jobs expire after 15 minutes, or can be discarded with <code>release_job</code>. Job results do not survive a server restart. Releasing a job does not remove an exported artifact. Summary caching retains serialized metadata only, bounded to 2 MiB and 16 entries, with invalidation by file revision.</p> <p>Cancellation is cooperative between records, XML validation events, and export writes. Reader initialization, decompression, and a single array decode can delay cancellation. A cancellation arriving after export publication can return a completed job with its artifact. Server shutdown requests cancellation and waits for worker cleanup.</p>"},{"location":"mcp/#exports-and-companion-packages","title":"Exports and companion packages","text":"<p>Export explicit record IDs with <code>export_records</code>, optionally through <code>start_job</code>. Each export uses a generated filename and never replaces another file. Missing or duplicate source IDs, source revision changes, cancellation before publication, and writes exceeding 100 MiB leave no published artifact. The source file is never modified.</p> <p>Record metadata includes a <code>structure</code> tree that retains nested acquisition terms, user parameters, and references, with binary arrays represented separately.</p> <p>The format is <code>mzmlpy-records-jsonl</code>, version 1. Its first line contains a manifest with file revision, package version, requested IDs, vocabularies, software, acquisition and processing metadata, and the binary representation. Subsequent lines contain selected records in file order, metadata, and original encoded binary text with its encoding parameters. Binary arrays are not decoded or processed. The result includes a local path, byte count, SHA-256 checksum, and an MCP export URI.</p> <p>The manifest's <code>record_list</code> preserves enclosing list defaults such as <code>defaultDataProcessingRef</code>.</p> <p><code>read_export</code> pages JSONL lines. The export resource returns the manifest. Very large lines must be consumed directly from the local artifact by a companion package. The format is an mzmlpy interchange contract, not an assertion that Spectacular already has an importer.</p> <p>Spectrum processing stays in Spectacular. This server does not calculate extracted ion chromatograms, pick peaks, smooth signals, normalize intensities, align runs, match spectra, or identify compounds. It does not render charts. Companion tools can consume the recorded arrays or exports to perform those tasks.</p>"},{"location":"mcp/#resources-and-prompts","title":"Resources and prompts","text":"<p>Four static MCP resources provide client context:</p> <ul> <li><code>mzmlpy://capabilities</code>: enabled capabilities, codec availability, and limits.</li> <li><code>mzmlpy://guide</code>: workflow, provenance, costs, and the processing boundary.</li> <li><code>mzmlpy://units</code>: unit conventions and numeric representations.</li> <li><code>mzmlpy://schemas</code>: the active tools' input and output schemas.</li> </ul> <p>With exports enabled, <code>mzmlpy://exports/{artifact_id}</code> exposes an artifact manifest. Three optional prompts guide user-selected workflows: <code>inspect_run</code>, <code>compare_acquisition</code>, and <code>prepare_handoff</code>. Prompts do not run tools or execute processing themselves.</p>"},{"location":"mcp/#operational-limits","title":"Operational limits","text":"<p>Readers use <code>in_memory=False</code> and streaming gzip access, with automatic use of embedded indexes. Each request opens its own reader. Plain files may need an index scan on open. Gzip queries and later pages can rescan earlier XML. Reading chromatogram metadata can pass the spectrum list. Page limits do not bound total I/O, elapsed time, or a single record's size. Array paging decodes full arrays before selecting values.</p> <p>Validation checks structure, references, counts, and array metadata, with explicit binary and XML offset options. It is not full XSD, ontology, or embedded gzip index validation. <code>issue_limit</code> bounds returned findings, not full-file work or internal validator storage. Serialized result envelopes are limited to 256 KiB before MCP encoding.</p> <p>Use trusted local files in directories that are not being modified concurrently. Resolved-path checks are not an operating-system sandbox. Exports require a trusted writable output directory. The server never follows external locations declared inside source metadata.</p>"},{"location":"api/chromatogram/","title":"Chromatogram","text":""},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram","title":"mzmlpy.spectra.Chromatogram  <code>dataclass</code>","text":"<pre><code>Chromatogram(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code>, <code>_BinaryDataArrayMixin</code></p> <p>An mzML <code>chromatogram</code> element.</p> <p>Exposes <code>time</code> and <code>intensity</code> binary arrays, optional <code>precursor</code> and <code>product</code> structures, and a <code>chromatogram_type</code> property (e.g. <code>\"tic\"</code>, <code>\"basepeak\"</code>, <code>\"srm\"</code>).</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get chromatogram id.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.id_dict","title":"id_dict  <code>property</code>","text":"<pre><code>id_dict: dict[str, int | str]\n</code></pre> <p>Parse the native id into its <code>key=value</code> components (integer values coerced to int).</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.default_array_length","title":"default_array_length  <code>property</code>","text":"<pre><code>default_array_length: int | None\n</code></pre> <p>Get chromatogram default array length, or None if not present.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.source_file_ref","title":"source_file_ref  <code>property</code>","text":"<pre><code>source_file_ref: str | None\n</code></pre> <p>Get chromatogram source file reference, or None if not present.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.time","title":"time  <code>property</code>","text":"<pre><code>time: np.ndarray | None\n</code></pre> <p>Get time array as a numpy array, or None if not present.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.intensity","title":"intensity  <code>property</code>","text":"<pre><code>intensity: np.ndarray | None\n</code></pre> <p>Get intensity array as a numpy array, or None if not present.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.has_precursor","title":"has_precursor  <code>property</code>","text":"<pre><code>has_precursor: bool\n</code></pre> <p>Check if this chromatogram has a precursor.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.precursor","title":"precursor  <code>property</code>","text":"<pre><code>precursor: Precursor | None\n</code></pre> <p>Get a Precursor object for the precursor of this chromatogram, or None if no precursor is present.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.has_product","title":"has_product  <code>property</code>","text":"<pre><code>has_product: bool\n</code></pre> <p>Check if this chromatogram has a product.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.product","title":"product  <code>property</code>","text":"<pre><code>product: Product | None\n</code></pre> <p>Get a Product object for the product of this chromatogram, or None if no product is present.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.data_processing_ref","title":"data_processing_ref  <code>property</code>","text":"<pre><code>data_processing_ref: str | None\n</code></pre> <p>Get chromatogram data processing reference, or None if not present.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.chromatogram_type","title":"chromatogram_type  <code>property</code>","text":"<pre><code>chromatogram_type: (\n    Literal[\n        \"emission\",\n        \"sim\",\n        \"basepeak\",\n        \"pic\",\n        \"tic\",\n        \"absorption\",\n        \"srm\",\n        \"sic\",\n    ]\n    | None\n)\n</code></pre> <p>Get chromatogram type (e.g. TIC, BPC, etc.) for this chromatogram.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.binary_arrays","title":"binary_arrays  <code>property</code>","text":"<pre><code>binary_arrays: list[BinaryDataArray]\n</code></pre> <p>Get a list of BinaryDataConverter objects for each binary data array.</p>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.get_binary_array","title":"get_binary_array","text":"<pre><code>get_binary_array(id: str) -&gt; BinaryDataArray | None\n</code></pre> <p>Get a BinaryDataConverter object for the binary data array with the specified id.</p> Source code in <code>src/mzmlpy/spectra.py</code> <pre><code>def get_binary_array(self, id: str) -&gt; BinaryDataArray | None:\n    \"\"\"Get a BinaryDataConverter object for the binary data array with the specified id.\"\"\"\n    if self._binary_array_list is not None:\n        return self._binary_array_list.get_binary_array(id)\n    return None\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.has_binary_array","title":"has_binary_array","text":"<pre><code>has_binary_array(id: str) -&gt; bool\n</code></pre> <p>Check if a binary data array with the specified id exists.</p> Source code in <code>src/mzmlpy/spectra.py</code> <pre><code>def has_binary_array(self, id: str) -&gt; bool:\n    \"\"\"Check if a binary data array with the specified id exists.\"\"\"\n    if self._binary_array_list is not None:\n        return self._binary_array_list.has_binary_array(id)\n    return False\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/chromatogram/#mzmlpy.spectra.Chromatogram.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/constants/","title":"Constants","text":""},{"location":"api/constants/#mzmlpy.constants","title":"mzmlpy.constants","text":""},{"location":"api/constants/#mzmlpy.constants.ION_MOBILITIES","title":"ION_MOBILITIES  <code>module-attribute</code>","text":"<pre><code>ION_MOBILITIES = {\n    BinaryDataArrayAccession.RAW_ION_MOBILITY,\n    BinaryDataArrayAccession.MEAN_ION_MOBILITY_DRIFT_TIME,\n    BinaryDataArrayAccession.DECONVOLUTED_ION_MOBILITY_DRIFT_TIME,\n    BinaryDataArrayAccession.MEAN_INVERSE_REDUCED_ION_MOBILITY,\n    BinaryDataArrayAccession.MEAN_ION_MOBILITY,\n    BinaryDataArrayAccession.DECONVOLUTED_INVERSE_REDUCED_ION_MOBILITY,\n    BinaryDataArrayAccession.RAW_ION_MOBILITY_DRIFT_TIME,\n    BinaryDataArrayAccession.RAW_INVERSE_REDUCED_ION_MOBILITY,\n    BinaryDataArrayAccession.ION_MOBILITY,\n}\n</code></pre>"},{"location":"api/constants/#mzmlpy.constants.PROTON_MASS","title":"PROTON_MASS  <code>module-attribute</code>","text":"<pre><code>PROTON_MASS = 1.00727646677\n</code></pre>"},{"location":"api/constants/#mzmlpy.constants.ScanPolarity","title":"ScanPolarity","text":"<p>               Bases: <code>StrEnum</code></p> <p>Enumeration of MS accessions for chromatogram properties.</p>"},{"location":"api/constants/#mzmlpy.constants.SpectrumType","title":"SpectrumType","text":"<p>               Bases: <code>StrEnum</code></p> <p>Enumeration of spectrum types.</p>"},{"location":"api/constants/#mzmlpy.constants.BinaryDataArrayAccession","title":"BinaryDataArrayAccession","text":"<p>               Bases: <code>StrEnum</code></p> <p>Enumeration of binary data array accessions.</p>"},{"location":"api/constants/#mzmlpy.constants.CompressionTypeAccessions","title":"CompressionTypeAccessions","text":"<p>               Bases: <code>StrEnum</code></p> <p>Enumeration of compression type accessions used in mzML binary data arrays.</p>"},{"location":"api/data-processing/","title":"Data Processing","text":""},{"location":"api/data-processing/#mzmlpy.elems.data_processing.DataProcessing","title":"mzmlpy.elems.data_processing.DataProcessing  <code>dataclass</code>","text":"<pre><code>DataProcessing(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_DataTreeWrapper</code></p> <p>Named collection of ordered processing methods describing how spectral data was transformed.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.DataProcessing.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get the data processing's <code>id</code> attribute.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the <code>id</code> attribute is missing.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.DataProcessing.processing_methods","title":"processing_methods  <code>property</code>","text":"<pre><code>processing_methods: tuple[ProcessingMethod, ...]\n</code></pre> <p>Get the ordered processing methods applied under this data processing.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.DataProcessing.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.DataProcessing.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.DataProcessing.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/data-processing/#processingmethod","title":"ProcessingMethod","text":""},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod","title":"mzmlpy.elems.data_processing.ProcessingMethod  <code>dataclass</code>","text":"<pre><code>ProcessingMethod(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A single data processing step applied to the spectral data, with an order index and software reference.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.order","title":"order  <code>property</code>","text":"<pre><code>order: int | None\n</code></pre> <p>Get this method's position in the processing order, if present.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.software_ref","title":"software_ref  <code>property</code>","text":"<pre><code>software_ref: str | None\n</code></pre> <p>Get the referenced software's <code>id</code>, if present.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/data-processing/#mzmlpy.elems.data_processing.ProcessingMethod.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/file-description/","title":"File Description","text":""},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription","title":"mzmlpy.elems.file_desc.FileDescription  <code>dataclass</code>","text":"<pre><code>FileDescription(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_DataTreeWrapper</code></p> <p>Top-level file description element containing content type, source files, and contacts.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription.file_content","title":"file_content  <code>property</code>","text":"<pre><code>file_content: FileContent | None\n</code></pre> <p>return file content as FileContent object if present</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription.source_files","title":"source_files  <code>property</code>","text":"<pre><code>source_files: list[SourceFile]\n</code></pre> <p>return source files as tuple of SourceFile objects</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription.contact","title":"contact  <code>property</code>","text":"<pre><code>contact: list[Contact]\n</code></pre> <p>return a list of contacts if present</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription.get_source_file","title":"get_source_file","text":"<pre><code>get_source_file(id: str) -&gt; SourceFile | None\n</code></pre> <p>Get a source file by its <code>id</code> attribute.</p> Source code in <code>src/mzmlpy/elems/file_desc.py</code> <pre><code>def get_source_file(self, id: str) -&gt; SourceFile | None:\n    \"\"\"Get a source file by its ``id`` attribute.\"\"\"\n    for sf in self.source_files:\n        if sf.id == id:\n            return sf\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileDescription.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/file-description/#filecontent","title":"FileContent","text":""},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent","title":"mzmlpy.elems.file_desc.FileContent  <code>dataclass</code>","text":"<pre><code>FileContent(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>CV parameters describing the overall content of the mzML file (e.g. MS1 spectrum, centroid spectrum).</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.dia_acquisition","title":"dia_acquisition  <code>property</code>","text":"<pre><code>dia_acquisition: DIAAcquisitionAccession | None\n</code></pre> <p>The DIA acquisition method declared for this file, or None if not a (declared) DIA file.</p> <p>Returns the merged-concept term the file content header carries (e.g. diaPASEF, SONAR, MSE) per the PSI IM-MS/DIA recommendation v1.0, \u00a73.3. Returns None for DDA files or DIA files that do not declare the method.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.is_dia","title":"is_dia  <code>property</code>","text":"<pre><code>is_dia: bool\n</code></pre> <p>Whether the file content header declares a data-independent acquisition method (\u00a73.3).</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.FileContent.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/file-description/#sourcefile","title":"SourceFile","text":""},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile","title":"mzmlpy.elems.file_desc.SourceFile  <code>dataclass</code>","text":"<pre><code>SourceFile(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A single source file referenced by the mzML document.</p> <p>Attributes are accessed via CV parameters (checksum type and value) as well as XML attributes (id, name, location).</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.id","title":"id  <code>property</code>","text":"<pre><code>id: str | None\n</code></pre> <p>Get the source file's <code>id</code> attribute.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.name","title":"name  <code>property</code>","text":"<pre><code>name: str | None\n</code></pre> <p>Get the source file's <code>name</code> attribute.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.location","title":"location  <code>property</code>","text":"<pre><code>location: str | None\n</code></pre> <p>Get the source file's <code>location</code> attribute (e.g. a directory URI).</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.checksum_type","title":"checksum_type  <code>property</code>","text":"<pre><code>checksum_type: Literal['MD5', 'SHA1', 'SHA256'] | None\n</code></pre> <p>Get checksum type if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.checksum","title":"checksum  <code>property</code>","text":"<pre><code>checksum: str | None\n</code></pre> <p>Get checksum value if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.SourceFile.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/file-description/#contact","title":"Contact","text":""},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact","title":"mzmlpy.elems.file_desc.Contact  <code>dataclass</code>","text":"<pre><code>Contact(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Contact information for a person or organization associated with the mzML file.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.name","title":"name  <code>property</code>","text":"<pre><code>name: str | None\n</code></pre> <p>Get the contact's name, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.organization","title":"organization  <code>property</code>","text":"<pre><code>organization: str | None\n</code></pre> <p>Get the contact's organization, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.address","title":"address  <code>property</code>","text":"<pre><code>address: str | None\n</code></pre> <p>Get the contact's address, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.url","title":"url  <code>property</code>","text":"<pre><code>url: str | None\n</code></pre> <p>Get the contact's URL, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.email","title":"email  <code>property</code>","text":"<pre><code>email: str | None\n</code></pre> <p>Get the contact's email address, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.phone_number","title":"phone_number  <code>property</code>","text":"<pre><code>phone_number: str | None\n</code></pre> <p>Get the contact's phone number, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.toll_free_phone_number","title":"toll_free_phone_number  <code>property</code>","text":"<pre><code>toll_free_phone_number: str | None\n</code></pre> <p>Get the contact's toll-free phone number, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.fax_number","title":"fax_number  <code>property</code>","text":"<pre><code>fax_number: str | None\n</code></pre> <p>Get the contact's fax number, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.role","title":"role  <code>property</code>","text":"<pre><code>role: str | None\n</code></pre> <p>Get the contact's role, if present.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/file-description/#mzmlpy.elems.file_desc.Contact.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/instrument/","title":"Instrument Configuration","text":""},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration","title":"mzmlpy.elems.instrument_config.InstrumentConfiguration  <code>dataclass</code>","text":"<pre><code>InstrumentConfiguration(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Complete description of a mass spectrometer hardware configuration.</p> <p>Includes source, analyzer, and detector components.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get the instrument configuration's <code>id</code> attribute.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the <code>id</code> attribute is missing.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.software_ref","title":"software_ref  <code>property</code>","text":"<pre><code>software_ref: str | None\n</code></pre> <p>Get the referenced software's <code>id</code>, if a software reference is present.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.source_components","title":"source_components  <code>property</code>","text":"<pre><code>source_components: tuple[SourceComponent, ...]\n</code></pre> <p>Get the ion source components of this instrument configuration.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.analyzer_components","title":"analyzer_components  <code>property</code>","text":"<pre><code>analyzer_components: tuple[AnalyzerComponent, ...]\n</code></pre> <p>Get the mass analyzer components of this instrument configuration.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.detector_components","title":"detector_components  <code>property</code>","text":"<pre><code>detector_components: tuple[DetectorComponent, ...]\n</code></pre> <p>Get the detector components of this instrument configuration.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.InstrumentConfiguration.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/instrument/#sourcecomponent","title":"SourceComponent","text":""},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent","title":"mzmlpy.elems.instrument_config.SourceComponent  <code>dataclass</code>","text":"<pre><code>SourceComponent(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_Component</code></p> <p>Ion source component of an instrument configuration (e.g. ESI, MALDI).</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.order","title":"order  <code>property</code>","text":"<pre><code>order: int | None\n</code></pre> <p>Get the component's position in the acquisition order, if present.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.SourceComponent.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/instrument/#analyzercomponent","title":"AnalyzerComponent","text":""},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent","title":"mzmlpy.elems.instrument_config.AnalyzerComponent  <code>dataclass</code>","text":"<pre><code>AnalyzerComponent(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_Component</code></p> <p>Mass analyzer component of an instrument configuration (e.g. quadrupole, Orbitrap).</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.order","title":"order  <code>property</code>","text":"<pre><code>order: int | None\n</code></pre> <p>Get the component's position in the acquisition order, if present.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.AnalyzerComponent.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/instrument/#detectorcomponent","title":"DetectorComponent","text":""},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent","title":"mzmlpy.elems.instrument_config.DetectorComponent  <code>dataclass</code>","text":"<pre><code>DetectorComponent(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_Component</code></p> <p>Detector component of an instrument configuration (e.g. electron multiplier, focal plane array).</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.order","title":"order  <code>property</code>","text":"<pre><code>order: int | None\n</code></pre> <p>Get the component's position in the acquisition order, if present.</p>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/instrument/#mzmlpy.elems.instrument_config.DetectorComponent.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/lookup/","title":"Lookup","text":""},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup","title":"mzmlpy.lookup.SpectrumLookup","text":"<pre><code>SpectrumLookup(\n    file_object: FileInterface,\n    count: int | None = None,\n    id_regex: str | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseLookup[Spectrum]</code></p> <p>Lookup interface for spectra.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def __init__(self, file_object: FileInterface, count: int | None = None, id_regex: str | None = None) -&gt; None:\n    self.file_object = file_object\n    self._count = count\n    self._id_regex = id_regex\n    self._cursor: Iterator[T] | None = None\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup.count","title":"count  <code>property</code>","text":"<pre><code>count: int | None\n</code></pre> <p>Get count of items.</p>"},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup.filter","title":"filter","text":"<pre><code>filter(\n    *,\n    ms_level: int | None = None,\n    retention_time: (\n        tuple[float | None, float | None] | None\n    ) = None,\n    polarity: Literal[\"positive\", \"negative\"] | None = None,\n    precursor_mz: (\n        tuple[float | None, float | None] | None\n    ) = None,\n    spectrum_type: (\n        Literal[\"centroid\", \"profile\"] | None\n    ) = None,\n    mobility_type: (\n        Literal[\"inverse_reduced\", \"drift_time\"] | None\n    ) = None,\n    ion_mobility: (\n        tuple[float | None, float | None] | None\n    ) = None,\n    faims_voltage: (\n        tuple[float | None, float | None] | None\n    ) = None\n) -&gt; Iterator[Spectrum]\n</code></pre> <p>Lazily select spectra by metadata, using inclusive time bounds in seconds.</p> <p>Criteria are combined with AND. Retention time matches any scan. Precursor m/z matches overlapping isolation windows, with selected ions as a fallback. Missing metadata does not match a requested criterion. Keep the reader open while iterating.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def filter(\n    self,\n    *,\n    ms_level: int | None = None,\n    retention_time: tuple[float | None, float | None] | None = None,\n    polarity: Literal[\"positive\", \"negative\"] | None = None,\n    precursor_mz: tuple[float | None, float | None] | None = None,\n    spectrum_type: Literal[\"centroid\", \"profile\"] | None = None,\n    mobility_type: Literal[\"inverse_reduced\", \"drift_time\"] | None = None,\n    ion_mobility: tuple[float | None, float | None] | None = None,\n    faims_voltage: tuple[float | None, float | None] | None = None,\n) -&gt; Iterator[Spectrum]:\n    \"\"\"Lazily select spectra by metadata, using inclusive time bounds in seconds.\n\n    Criteria are combined with AND. Retention time matches any scan. Precursor m/z\n    matches overlapping isolation windows, with selected ions as a fallback. Missing\n    metadata does not match a requested criterion. Keep the reader open while iterating.\n    \"\"\"\n    predicate = SpectrumFilter(\n        ms_level, retention_time, polarity, precursor_mz, spectrum_type, mobility_type, ion_mobility, faims_voltage\n    )\n    return (spectrum for spectrum in self if predicate.matches(spectrum))\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup.get_by_index","title":"get_by_index","text":"<pre><code>get_by_index(index: int | str) -&gt; T\n</code></pre> <p>Get item by index. Negative indices count from the end, like a list.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def get_by_index(self, index: int | str) -&gt; T:\n    \"\"\"Get item by index. Negative indices count from the end, like a list.\"\"\"\n    if isinstance(index, str):\n        index = int(index)\n    if index &lt; 0:\n        # Normalize against the count so lookup[-1] mirrors slice behavior (lookup[-1:]).\n        count = self.count\n        if count is not None:\n            index += count\n        if index &lt; 0:\n            raise IndexError(\"Index out of range\")\n    return self._get_by_index_impl(index)\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup.get_by_id","title":"get_by_id","text":"<pre><code>get_by_id(identifier: str) -&gt; T\n</code></pre> <p>Get item by ID.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def get_by_id(self, identifier: str) -&gt; T:\n    \"\"\"Get item by ID.\"\"\"\n    return self._get_by_id_impl(identifier)\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup.get_by_slice","title":"get_by_slice","text":"<pre><code>get_by_slice(slice_obj: slice) -&gt; list[T]\n</code></pre> <p>Get items by slice notation.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def get_by_slice(self, slice_obj: slice) -&gt; list[T]:\n    \"\"\"Get items by slice notation.\"\"\"\n    if self.count is None:\n        # Don't know count - must iterate all and slice\n        items: list[T] = list(self)\n        return items[slice_obj]\n    # Know count - use slice.indices() to handle all cases\n    start, stop, step = slice_obj.indices(self.count)\n    return [self.get_by_index(i) for i in range(start, stop, step)]\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup.next","title":"next","text":"<pre><code>next() -&gt; T\n</code></pre> <p>Advance a persistent cursor and return the next item.</p> <p>The cursor is created on first call and advances on each subsequent call, raising <code>StopIteration</code> once every item has been returned. Call :meth:<code>reset</code> to start over.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def next(self) -&gt; T:\n    \"\"\"Advance a persistent cursor and return the next item.\n\n    The cursor is created on first call and advances on each subsequent call, raising\n    ``StopIteration`` once every item has been returned. Call :meth:`reset` to start over.\n    \"\"\"\n    if self._cursor is None:\n        self._cursor = iter(self)\n    return next(self._cursor)\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.SpectrumLookup.reset","title":"reset","text":"<pre><code>reset() -&gt; None\n</code></pre> <p>Reset the cursor used by :meth:<code>next</code> so the next call starts from the first item.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def reset(self) -&gt; None:\n    \"\"\"Reset the cursor used by :meth:`next` so the next call starts from the first item.\"\"\"\n    self._cursor = None\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup","title":"mzmlpy.lookup.ChromatogramLookup","text":"<pre><code>ChromatogramLookup(\n    file_object: FileInterface,\n    count: int | None = None,\n    id_regex: str | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseLookup[Chromatogram]</code></p> <p>Lookup interface for chromatograms.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def __init__(self, file_object: FileInterface, count: int | None = None, id_regex: str | None = None) -&gt; None:\n    self.file_object = file_object\n    self._count = count\n    self._id_regex = id_regex\n    self._cursor: Iterator[T] | None = None\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup.TIC","title":"TIC  <code>property</code>","text":"<pre><code>TIC: Chromatogram\n</code></pre> <p>Access Total Ion Chromatogram.</p>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup.count","title":"count  <code>property</code>","text":"<pre><code>count: int | None\n</code></pre> <p>Get count of items.</p>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup.get_by_index","title":"get_by_index","text":"<pre><code>get_by_index(index: int | str) -&gt; T\n</code></pre> <p>Get item by index. Negative indices count from the end, like a list.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def get_by_index(self, index: int | str) -&gt; T:\n    \"\"\"Get item by index. Negative indices count from the end, like a list.\"\"\"\n    if isinstance(index, str):\n        index = int(index)\n    if index &lt; 0:\n        # Normalize against the count so lookup[-1] mirrors slice behavior (lookup[-1:]).\n        count = self.count\n        if count is not None:\n            index += count\n        if index &lt; 0:\n            raise IndexError(\"Index out of range\")\n    return self._get_by_index_impl(index)\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup.get_by_id","title":"get_by_id","text":"<pre><code>get_by_id(identifier: str) -&gt; T\n</code></pre> <p>Get item by ID.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def get_by_id(self, identifier: str) -&gt; T:\n    \"\"\"Get item by ID.\"\"\"\n    return self._get_by_id_impl(identifier)\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup.get_by_slice","title":"get_by_slice","text":"<pre><code>get_by_slice(slice_obj: slice) -&gt; list[T]\n</code></pre> <p>Get items by slice notation.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def get_by_slice(self, slice_obj: slice) -&gt; list[T]:\n    \"\"\"Get items by slice notation.\"\"\"\n    if self.count is None:\n        # Don't know count - must iterate all and slice\n        items: list[T] = list(self)\n        return items[slice_obj]\n    # Know count - use slice.indices() to handle all cases\n    start, stop, step = slice_obj.indices(self.count)\n    return [self.get_by_index(i) for i in range(start, stop, step)]\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup.next","title":"next","text":"<pre><code>next() -&gt; T\n</code></pre> <p>Advance a persistent cursor and return the next item.</p> <p>The cursor is created on first call and advances on each subsequent call, raising <code>StopIteration</code> once every item has been returned. Call :meth:<code>reset</code> to start over.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def next(self) -&gt; T:\n    \"\"\"Advance a persistent cursor and return the next item.\n\n    The cursor is created on first call and advances on each subsequent call, raising\n    ``StopIteration`` once every item has been returned. Call :meth:`reset` to start over.\n    \"\"\"\n    if self._cursor is None:\n        self._cursor = iter(self)\n    return next(self._cursor)\n</code></pre>"},{"location":"api/lookup/#mzmlpy.lookup.ChromatogramLookup.reset","title":"reset","text":"<pre><code>reset() -&gt; None\n</code></pre> <p>Reset the cursor used by :meth:<code>next</code> so the next call starts from the first item.</p> Source code in <code>src/mzmlpy/lookup.py</code> <pre><code>def reset(self) -&gt; None:\n    \"\"\"Reset the cursor used by :meth:`next` so the next call starts from the first item.\"\"\"\n    self._cursor = None\n</code></pre>"},{"location":"api/metadata/","title":"Metadata","text":""},{"location":"api/metadata/#software","title":"Software","text":""},{"location":"api/metadata/#mzmlpy.elems.software.Software","title":"mzmlpy.elems.software.Software  <code>dataclass</code>","text":"<pre><code>Software(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Software used to acquire or process the data, identified by id and version.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get the software's <code>id</code> attribute.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the <code>id</code> attribute is missing.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.version","title":"version  <code>property</code>","text":"<pre><code>version: str | None\n</code></pre> <p>Get the software's version string, if present.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.software.Software.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/metadata/#sample","title":"Sample","text":""},{"location":"api/metadata/#mzmlpy.elems.sample.Sample","title":"mzmlpy.elems.sample.Sample  <code>dataclass</code>","text":"<pre><code>Sample(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A sample analyzed in the experiment, described by id, optional name, and CV parameters.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get the sample's <code>id</code> attribute.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the <code>id</code> attribute is missing.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.name","title":"name  <code>property</code>","text":"<pre><code>name: str | None\n</code></pre> <p>Get the sample's name, if present.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.sample.Sample.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/metadata/#run","title":"Run","text":""},{"location":"api/metadata/#mzmlpy.elems.run.Run","title":"mzmlpy.elems.run.Run  <code>dataclass</code>","text":"<pre><code>Run(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>The mzML run element containing instrument configuration, sample, and acquisition timestamp references.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.id","title":"id  <code>property</code>","text":"<pre><code>id: str | None\n</code></pre> <p>Get the run's <code>id</code> attribute.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.default_instrument_configuration_ref","title":"default_instrument_configuration_ref  <code>property</code>","text":"<pre><code>default_instrument_configuration_ref: str | None\n</code></pre> <p>Get the <code>id</code> of the default instrument configuration for this run, if present.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.default_source_file_ref","title":"default_source_file_ref  <code>property</code>","text":"<pre><code>default_source_file_ref: str | None\n</code></pre> <p>Get the <code>id</code> of the default source file for this run, if present.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.sample_ref","title":"sample_ref  <code>property</code>","text":"<pre><code>sample_ref: str | None\n</code></pre> <p>Get the <code>id</code> of the sample analyzed in this run, if present.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.start_time_stamp","title":"start_time_stamp  <code>property</code>","text":"<pre><code>start_time_stamp: datetime.datetime | None\n</code></pre> <p>Get the run's acquisition start time, if present and parseable.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.run.Run.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/metadata/#scansetting","title":"ScanSetting","text":""},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting","title":"mzmlpy.elems.scan_setting.ScanSetting  <code>dataclass</code>","text":"<pre><code>ScanSetting(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Scan acquisition settings including source file references and target m/z lists.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get the scan setting's <code>id</code> attribute.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the <code>id</code> attribute is missing.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.source_file_refs","title":"source_file_refs  <code>property</code>","text":"<pre><code>source_file_refs: tuple[SourceFileRef, ...]\n</code></pre> <p>Get the referenced source files' <code>id</code>s for this scan setting.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.targets","title":"targets  <code>property</code>","text":"<pre><code>targets: tuple[Target, ...]\n</code></pre> <p>Get the targeted m/z entries for this scan setting.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.ScanSetting.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/metadata/#target","title":"Target","text":""},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target","title":"mzmlpy.elems.scan_setting.Target  <code>dataclass</code>","text":"<pre><code>Target(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A targeted m/z entry within a scan settings target list.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.mz","title":"mz  <code>property</code>","text":"<pre><code>mz: float | None\n</code></pre> <p>Get the targeted m/z value, if present.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.scan_setting.Target.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/metadata/#sourcefileref","title":"SourceFileRef","text":""},{"location":"api/metadata/#mzmlpy.elems.scan_setting.SourceFileRef","title":"mzmlpy.elems.scan_setting.SourceFileRef  <code>dataclass</code>","text":"<pre><code>SourceFileRef(ref: str)\n</code></pre> <p>A reference to a source file by its id string.</p>"},{"location":"api/metadata/#referenceableparamgroup","title":"ReferenceableParamGroup","text":""},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup","title":"mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup  <code>dataclass</code>","text":"<pre><code>ReferenceableParamGroup(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A named group of CV and user parameters that can be referenced by other elements to avoid repetition.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get the param group's <code>id</code> attribute, used by other elements to reference it.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the <code>id</code> attribute is missing.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/metadata/#mzmlpy.elems.referenceable_param_group.ReferenceableParamGroup.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/metadata/#referenceableparamgroupref","title":"ReferenceableParamGroupRef","text":""},{"location":"api/metadata/#mzmlpy.elems.params.ReferenceableParamGroupRef","title":"mzmlpy.elems.params.ReferenceableParamGroupRef  <code>dataclass</code>","text":"<pre><code>ReferenceableParamGroupRef(ref: str)\n</code></pre> <p>A reference to a referenceable parameter group by its id string.</p>"},{"location":"api/mzml/","title":"Mzml Reader","text":""},{"location":"api/mzml/#mzmlpy.run.Mzml","title":"mzmlpy.run.Mzml","text":"<pre><code>Mzml(\n    file: str | Path | Any,\n    build_index_from_scratch: bool = False,\n    gzip_mode: Literal[\n        \"auto\", \"extract\", \"indexed\", \"stream\"\n    ] = \"auto\",\n    in_memory: bool = True,\n    extract_dir: str | Path | None = None,\n    spectrum_id_regex: str | None = None,\n    chromatogram_id_regex: str | None = None,\n)\n</code></pre> <p>Reader for mzML files.</p> <p>Data is lazily loaded, so only the specific sections of the XML file are parsed. The actual data and properties of objects are only parsed when accessed. Use the context manager to ensure proper file handling. The <code>spectra</code> and <code>chromatograms</code> properties return lookup objects that support iteration, indexing, and ID-based access.</p> Note <p>A reader is not thread-safe: random access shares a single underlying file handle, so concurrent access from multiple threads on the same <code>Mzml</code> instance will interleave seeks and reads and return corrupt or wrong data. Use one reader per thread.</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str | Path | Any</code> <p>Path to the mzML file (str or Path) or a file-like object.</p> required <code>build_index_from_scratch</code> <code>bool</code> <p>Build the index from scratch instead of using an existing index.</p> <code>False</code> <code>gzip_mode</code> <code>Literal['auto', 'extract', 'indexed', 'stream']</code> <p>Strategy for reading gzip-compressed (<code>.mzML.gz</code>) files:</p> <p>Self-indexed files created by :func:<code>mzmlpy.write_indexed_gzip</code> are detected automatically when <code>in_memory=False</code>. They use their embedded index regardless of this setting.</p> <ul> <li><code>\"auto\"</code> (default): Use an embedded index, a current extracted cache, or   complete rapidgzip sidecars in that order. Extract when none is available.</li> <li><code>\"extract\"</code>: Decompress to a temporary file on disk, then use   standard random-access reading.</li> <li><code>\"indexed\"</code>: Use the <code>rapidgzip</code> library for seekable access to the   compressed file without extracting to disk. Requires   <code>pip install mzmlpy[rapidgzip]</code>.</li> <li><code>\"stream\"</code>: Stream the file sequentially without building an index.   Individual spectrum access re-scans the file from the beginning each time.</li> </ul> <code>'auto'</code> <code>in_memory</code> <code>bool</code> <p>Load the entire file into memory for faster access.</p> <code>True</code> <code>extract_dir</code> <code>str | Path | None</code> <p>Directory to store extracted <code>.mzML</code> files when using <code>gzip_mode='extract'</code>. If <code>None</code> (default), a system temp directory is used (<code>&lt;tmpdir&gt;/mzmlpy/</code>). Set this to a custom path to manage extracted files yourself \u2014 useful for batch processing where you want to extract all files to one directory and clean up afterward.</p> <code>None</code> <code>spectrum_id_regex</code> <code>str | None</code> <p>Optional regex applied to spectrum IDs to create a secondary lookup key. The first capture group (or full match if no groups) becomes the simplified key. For example, <code>r\"scan=(\\d+)\"</code> lets you look up spectra by scan number (<code>reader.spectra[\"19\"]</code>) instead of the full native ID (<code>\"scan=19\"</code>).</p> <code>None</code> <code>chromatogram_id_regex</code> <code>str | None</code> <p>Optional regex applied to chromatogram IDs to create a secondary lookup key. Works identically to <code>spectrum_id_regex</code> but for chromatograms.</p> <code>None</code> <p>Initialize Mzml and parse metadata.</p> Source code in <code>src/mzmlpy/run.py</code> <pre><code>def __init__(\n    self,\n    file: str | Path | Any,\n    build_index_from_scratch: bool = False,\n    gzip_mode: Literal[\"auto\", \"extract\", \"indexed\", \"stream\"] = \"auto\",\n    in_memory: bool = True,\n    extract_dir: str | Path | None = None,\n    spectrum_id_regex: str | None = None,\n    chromatogram_id_regex: str | None = None,\n) -&gt; None:\n    \"\"\"Initialize Mzml and parse metadata.\"\"\"\n    self._spectrum_id_regex = spectrum_id_regex\n    self._chromatogram_id_regex = chromatogram_id_regex\n    self._spectra_lookup: SpectrumLookup | None = None\n    self._chromatograms_lookup: ChromatogramLookup | None = None\n    self._path: Path | None = None\n    file_interface_arg: Any\n\n    if isinstance(file, str | Path):\n        self._path = Path(file)\n        # Use string representation for internal helpers that expect paths\n        path_str = str(self._path)\n        self._encoding = _determine_file_encoding(path_str)\n        file_interface_arg = path_str\n    else:\n        # File-like object \u2014 must be a readable binary stream. Validate up front so an\n        # unsupported input (e.g. an int) raises a clear TypeError instead of an opaque\n        # AttributeError from encoding sniffing below.\n        if not (hasattr(file, \"read\") and hasattr(file, \"readline\")):\n            raise TypeError(\n                f\"Unsupported input type {type(file).__name__!r}: expected a path (str/Path) \"\n                \"or a readable binary file-like object.\"\n            )\n        if hasattr(file, \"name\") and isinstance(file.name, str):\n            self._path = Path(file.name)\n        self._encoding = _guess_encoding(file)\n        file_interface_arg = file\n\n    # Open file\n    self._file_object: FileInterface = FileInterface(\n        path=file_interface_arg,\n        encoding=self._encoding,\n        build_index_from_scratch=build_index_from_scratch,\n        gzip_mode=gzip_mode,\n        in_memory=in_memory,\n        extract_dir=str(extract_dir) if extract_dir is not None else None,\n    )\n\n    # Parse metadata. If parsing fails, close the file object so a half-constructed\n    # reader does not leak extracted temp files or rapidgzip worker threads \u2014 the caller\n    # never receives the object, so it can never call close() itself.\n    try:\n        self._root, self.iter, builder = self._parse_metadata()\n        # Extract parsed content\n        self._content: _MzMLContent = builder.build()\n        self.obo_version = builder.obo_version\n    except BaseException:\n        self._file_object.close()\n        raise\n</code></pre>"},{"location":"api/mzml/#mzmlpy.run.Mzml.access_strategy","title":"access_strategy  <code>property</code>","text":"<pre><code>access_strategy: AccessStrategy\n</code></pre> <p>Concrete storage strategy selected when the file was opened.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.file_path","title":"file_path  <code>property</code>","text":"<pre><code>file_path: Path | None\n</code></pre> <p>Access the file path as a Path object if available.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.file_name","title":"file_name  <code>property</code>","text":"<pre><code>file_name: str\n</code></pre> <p>Access the file name as a string.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.spectra","title":"spectra  <code>property</code>","text":"<pre><code>spectra: SpectrumLookup\n</code></pre> <p>Access spectra lookup.</p> <p>Returns the same lookup instance across calls so its <code>next()</code>/<code>reset()</code> cursor and regex <code>_id_map</code> persist \u2014 <code>reader.spectra.next()</code> in a loop advances instead of restarting, and ID lookups don't re-scan the file on every access.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.chromatograms","title":"chromatograms  <code>property</code>","text":"<pre><code>chromatograms: ChromatogramLookup\n</code></pre> <p>Access chromatograms lookup.</p> <p>Returns the same lookup instance across calls (see :meth:<code>spectra</code>).</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.TIC","title":"TIC  <code>property</code>","text":"<pre><code>TIC: Chromatogram | None\n</code></pre> <p>Access the Total Ion Chromatogram (TIC).</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Access mzML id.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.version","title":"version  <code>property</code>","text":"<pre><code>version: str\n</code></pre> <p>Access mzML version.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.cvs","title":"cvs  <code>property</code>","text":"<pre><code>cvs: dict[str, CVElement]\n</code></pre> <p>Access controlled vocabularies.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.file_description","title":"file_description  <code>property</code>","text":"<pre><code>file_description: FileDescription | None\n</code></pre> <p>Access file description.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.referenceable_param_groups","title":"referenceable_param_groups  <code>property</code>","text":"<pre><code>referenceable_param_groups: dict[\n    str, ReferenceableParamGroup\n]\n</code></pre> <p>Access referenceable parameter groups.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.softwares","title":"softwares  <code>property</code>","text":"<pre><code>softwares: dict[str, Software]\n</code></pre> <p>Access software list.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.instrument_configurations","title":"instrument_configurations  <code>property</code>","text":"<pre><code>instrument_configurations: dict[\n    str, InstrumentConfiguration\n]\n</code></pre> <p>Access instrument configurations.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.data_processes","title":"data_processes  <code>property</code>","text":"<pre><code>data_processes: dict[str, DataProcessing]\n</code></pre> <p>Access data processing steps.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.samples","title":"samples  <code>property</code>","text":"<pre><code>samples: dict[str, Sample]\n</code></pre> <p>Access sample list.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.scan_settings","title":"scan_settings  <code>property</code>","text":"<pre><code>scan_settings: dict[str, ScanSetting]\n</code></pre> <p>Access scan settings.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.run","title":"run  <code>property</code>","text":"<pre><code>run: Run | None\n</code></pre> <p>Access run information.</p>"},{"location":"api/mzml/#mzmlpy.run.Mzml.validate","title":"validate","text":"<pre><code>validate(\n    *,\n    decode_binary: bool = False,\n    check_index: bool = False\n) -&gt; ValidationReport\n</code></pre> <p>Validate this reader's XML through a fresh handle, preserving its lookup cursor.</p> <p>See :func:<code>mzmlpy.validate</code> for check scope. This checks the representation selected by the reader. Use the standalone function to validate the original file directly.</p> Source code in <code>src/mzmlpy/run.py</code> <pre><code>def validate(self, *, decode_binary: bool = False, check_index: bool = False) -&gt; ValidationReport:\n    \"\"\"Validate this reader's XML through a fresh handle, preserving its lookup cursor.\n\n    See :func:`mzmlpy.validate` for check scope. This checks the representation selected\n    by the reader. Use the standalone function to validate the original file directly.\n    \"\"\"\n    with self._file_object.file_handler.get_file_handler(self._encoding) as handle:\n        binary = cast(BinaryIO, getattr(handle, \"buffer\", handle))\n        return _validate_stream(binary, decode_binary=decode_binary, check_index=check_index)\n</code></pre>"},{"location":"api/mzml/#utilities","title":"Utilities","text":""},{"location":"api/mzml/#mzmlpy.run.peek_spectrum_count","title":"mzmlpy.run.peek_spectrum_count","text":"<pre><code>peek_spectrum_count(file: str | Path) -&gt; int | None\n</code></pre> <p>Return a file's spectrum count without building a random-access index.</p> <p>Unlike <code>Mzml(file).spectrum_count</code>, this does not construct a reader or index every spectrum's byte offset \u2014 it streams forward just far enough to read the <code>&lt;spectrumList count=\"N\"&gt;</code> opening tag's <code>count</code> attribute (typically a few KB into the file, well before the header content is complete) and stops. Useful for cheaply checking many files (e.g. before deciding which to open fully). Returns <code>None</code> if the file has no <code>spectrumList</code> or the tag has no <code>count</code> attribute.</p> Note <p>There is no equally cheap <code>peek_chromatogram_count</code>: per the mzML schema, <code>chromatogramList</code> follows <code>spectrumList</code>, so reaching its opening tag requires streaming past the entire spectrum list first \u2014 at that point building the full index via :class:<code>Mzml</code> is a better fit than a \"peek.\"</p> Source code in <code>src/mzmlpy/run.py</code> <pre><code>def peek_spectrum_count(file: str | Path) -&gt; int | None:\n    \"\"\"Return a file's spectrum count without building a random-access index.\n\n    Unlike ``Mzml(file).spectrum_count``, this does not construct a reader or index every\n    spectrum's byte offset \u2014 it streams forward just far enough to read the\n    ``&lt;spectrumList count=\"N\"&gt;`` opening tag's ``count`` attribute (typically a few KB into the\n    file, well before the header content is complete) and stops. Useful for cheaply checking many\n    files (e.g. before deciding which to open fully). Returns ``None`` if the file has no\n    ``spectrumList`` or the tag has no ``count`` attribute.\n\n    Note:\n        There is no equally cheap ``peek_chromatogram_count``: per the mzML schema,\n        ``chromatogramList`` follows ``spectrumList``, so reaching its opening tag requires\n        streaming past the entire spectrum list first \u2014 at that point building the full index\n        via :class:`Mzml` is a better fit than a \"peek.\"\n    \"\"\"\n    path_str = str(file)\n    is_gz = path_str.endswith(\".gz\") or path_str.endswith(\".igz\")\n    file_handle = gzip_open_binary(path_str) if is_gz else open(path_str, \"rb\")\n    try:\n        # Read the count off the spectrumList *start* tag, but clear completed elements on their\n        # *end* events so that a file with no spectrumList doesn't accumulate the whole tree in\n        # memory before returning None.\n        for event, element in ElementTree.iterparse(file_handle, events=(\"start\", \"end\")):\n            if event == \"start\":\n                if get_tag(element) == MzMLElement.SPECTRUM_LIST:\n                    count = element.attrib.get(\"count\")\n                    return int(count) if count is not None else None\n            else:\n                element.clear()\n        return None\n    finally:\n        file_handle.close()\n</code></pre>"},{"location":"api/mzml/#mzmlpy.util.clear_cache","title":"mzmlpy.util.clear_cache","text":"<pre><code>clear_cache() -&gt; None\n</code></pre> <p>Remove all cached files from the mzmlpy temporary directory.</p> <p>Deletes the <code>&lt;tmpdir&gt;/mzmlpy/</code> directory and all its contents. This includes extracted <code>.mzML</code> files created by <code>gzip_mode='extract'</code>.</p> <p>Example::</p> <pre><code>from mzmlpy import clear_cache\nclear_cache()\n</code></pre> Source code in <code>src/mzmlpy/util.py</code> <pre><code>def clear_cache() -&gt; None:\n    \"\"\"Remove all cached files from the mzmlpy temporary directory.\n\n    Deletes the ``&lt;tmpdir&gt;/mzmlpy/`` directory and all its contents.\n    This includes extracted ``.mzML`` files created by ``gzip_mode='extract'``.\n\n    Example::\n\n        from mzmlpy import clear_cache\n        clear_cache()\n    \"\"\"\n    cache_dir = _get_cache_dir()\n    if os.path.isdir(cache_dir):\n        shutil.rmtree(cache_dir)\n</code></pre>"},{"location":"api/params/","title":"CV Parameters","text":""},{"location":"api/params/#mzmlpy.elems.params.CvParam","title":"mzmlpy.elems.params.CvParam  <code>dataclass</code>","text":"<pre><code>CvParam(\n    name: str,\n    value: str | None,\n    unit_accession: str | None,\n    unit_name: str | None,\n    unit_cv_ref: str | None,\n    cv_ref: str,\n    accession: str,\n)\n</code></pre> <p>               Bases: <code>_Param</code></p> <p>A controlled vocabulary parameter with a CV reference and accession number.</p>"},{"location":"api/params/#mzmlpy.elems.params.CvParam.to_timedelta","title":"to_timedelta  <code>property</code>","text":"<pre><code>to_timedelta: timedelta | None\n</code></pre> <p>Convert this CvParam to a timedelta object if it has a time unit, otherwise return None.</p>"},{"location":"api/params/#userparam","title":"UserParam","text":""},{"location":"api/params/#mzmlpy.elems.params.UserParam","title":"mzmlpy.elems.params.UserParam  <code>dataclass</code>","text":"<pre><code>UserParam(\n    name: str,\n    value: str | None,\n    unit_accession: str | None,\n    unit_name: str | None,\n    unit_cv_ref: str | None,\n    type_value: str | None,\n)\n</code></pre> <p>               Bases: <code>_Param</code></p> <p>A user-defined parameter with an arbitrary name and optional type annotation.</p>"},{"location":"api/params/#mzmlpy.elems.params.UserParam.to_timedelta","title":"to_timedelta  <code>property</code>","text":"<pre><code>to_timedelta: timedelta | None\n</code></pre> <p>Convert this CvParam to a timedelta object if it has a time unit, otherwise return None.</p>"},{"location":"api/params/#cvelement","title":"CVElement","text":""},{"location":"api/params/#mzmlpy.content.CVElement","title":"mzmlpy.content.CVElement","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Named tuple for controlled vocabulary elements.</p>"},{"location":"api/precursor/","title":"Precursor &amp; Ions","text":""},{"location":"api/precursor/#mzmlpy.spectra.Precursor","title":"mzmlpy.spectra.Precursor  <code>dataclass</code>","text":"<pre><code>Precursor(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_DataTreeWrapper</code></p> <p>Represents a precursor element in an mzML spectrum.</p> <p>Provides access to the isolation window, selected ions, activation parameters, and reference attributes such as spectrum ref and source file ref.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Precursor.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Precursor.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Precursor.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/precursor/#product","title":"Product","text":""},{"location":"api/precursor/#mzmlpy.spectra.Product","title":"mzmlpy.spectra.Product  <code>dataclass</code>","text":"<pre><code>Product(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A product ion selection element containing an isolation window and CV parameters.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.isolation_window","title":"isolation_window  <code>property</code>","text":"<pre><code>isolation_window: IsolationWindow | None\n</code></pre> <p>Get the isolation window for this product, if present.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Product.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Product.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/precursor/#isolationwindow","title":"IsolationWindow","text":""},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow","title":"mzmlpy.spectra.IsolationWindow  <code>dataclass</code>","text":"<pre><code>IsolationWindow(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Represents an isolation window element from a precursor or product.</p> <p>Provides access to the target m/z, lower offset, and upper offset values.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.target_mz","title":"target_mz  <code>property</code>","text":"<pre><code>target_mz: float | None\n</code></pre> <p>Get isolation window target m/z for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.lower_offset","title":"lower_offset  <code>property</code>","text":"<pre><code>lower_offset: float | None\n</code></pre> <p>Get isolation window lower offset for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.upper_offset","title":"upper_offset  <code>property</code>","text":"<pre><code>upper_offset: float | None\n</code></pre> <p>Get isolation window upper offset for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.no_isolation","title":"no_isolation  <code>property</code>","text":"<pre><code>no_isolation: bool\n</code></pre> <p>Whether this window carries the \"no isolation\" marker (MS:1003159).</p> <p>Full-mass-range DIA (e.g. MSE/HDMSE) sets this on an otherwise-empty isolationWindow to signal that no precursor was isolated (PSI IM-MS/DIA recommendation v1.0, \u00a73.5).</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.IsolationWindow.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/precursor/#selectedion","title":"SelectedIon","text":""},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon","title":"mzmlpy.spectra.SelectedIon  <code>dataclass</code>","text":"<pre><code>SelectedIon(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Represents a selected ion element within a precursor.</p> <p>Provides access to the selected ion m/z, peak intensity, charge state, ion mobility values, FAIMS voltages, and collisional cross section.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.selected_ion_mz","title":"selected_ion_mz  <code>property</code>","text":"<pre><code>selected_ion_mz: float | None\n</code></pre> <p>Get selected ion m/z for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.peak_intensity","title":"peak_intensity  <code>property</code>","text":"<pre><code>peak_intensity: float | None\n</code></pre> <p>Get peak intensity for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.charge_state","title":"charge_state  <code>property</code>","text":"<pre><code>charge_state: int | None\n</code></pre> <p>Get charge state for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.ir_im","title":"ir_im  <code>property</code>","text":"<pre><code>ir_im: float | None\n</code></pre> <p>Get inversion reduced ion mobility for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.im_drift_time","title":"im_drift_time  <code>property</code>","text":"<pre><code>im_drift_time: float | None\n</code></pre> <p>Get ion mobility drift time for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.faims_voltage_start","title":"faims_voltage_start  <code>property</code>","text":"<pre><code>faims_voltage_start: float | None\n</code></pre> <p>Get FAIMS voltage start for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.faims_voltage_end","title":"faims_voltage_end  <code>property</code>","text":"<pre><code>faims_voltage_end: float | None\n</code></pre> <p>Get FAIMS voltage end for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.ccs","title":"ccs  <code>property</code>","text":"<pre><code>ccs: float | None\n</code></pre> <p>Get collisional cross section for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.SelectedIon.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/precursor/#activation","title":"Activation","text":""},{"location":"api/precursor/#mzmlpy.spectra.Activation","title":"mzmlpy.spectra.Activation  <code>dataclass</code>","text":"<pre><code>Activation(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Represents an activation element within a precursor.</p> <p>Provides access to the activation type, collision energy, supplemental collision energy, collision gas, and collision gas pressure.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.activation_type","title":"activation_type  <code>property</code>","text":"<pre><code>activation_type: CollisionDissociationTypeAccession | None\n</code></pre> <p>Get activation type for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.activation_energy","title":"activation_energy  <code>property</code>","text":"<pre><code>activation_energy: float | None\n</code></pre> <p>Get activation energy for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.ce","title":"ce  <code>property</code>","text":"<pre><code>ce: float | None\n</code></pre> <p>Get collision energy for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.supplemental_ce","title":"supplemental_ce  <code>property</code>","text":"<pre><code>supplemental_ce: float | None\n</code></pre> <p>Get supplemental collision energy for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.collision_gas","title":"collision_gas  <code>property</code>","text":"<pre><code>collision_gas: str | None\n</code></pre> <p>Get collision gas for this precursor.</p> <p><code>collision gas</code> (MS:1000419) is normally a valueless flag whose identity is carried by the term name, so this returns the parameter's value when one is present and otherwise the term name \u2014 instead of returning <code>None</code> for the (common) valueless case.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.collision_gas_pressure","title":"collision_gas_pressure  <code>property</code>","text":"<pre><code>collision_gas_pressure: float | None\n</code></pre> <p>Get collision gas pressure for this precursor.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/precursor/#mzmlpy.spectra.Activation.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/spectrum/","title":"Spectrum","text":""},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum","title":"mzmlpy.spectra.Spectrum  <code>dataclass</code>","text":"<pre><code>Spectrum(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code>, <code>_BinaryDataArrayMixin</code>, <code>_ScanListMixin</code>, <code>_PrecursorListMixin</code>, <code>_ProductListMixin</code></p> <p>An mzML <code>spectrum</code> element.</p> <p>Exposes binary data arrays (<code>mz</code>, <code>intensity</code>, <code>charge</code>, ion mobility via <code>has_im</code>/<code>im_types</code>), scan metadata (<code>scan_start_time</code>, <code>ion_injection_time</code>, <code>lower_mz</code>, <code>upper_mz</code>, <code>spectrum_type</code>, <code>polarity</code>, <code>ms_level</code>, <code>TIC</code>), and structured precursor/product lists.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Get spectrum id.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.id_dict","title":"id_dict  <code>property</code>","text":"<pre><code>id_dict: dict[str, int | str]\n</code></pre> <p>Parse the native id into its <code>key=value</code> components (e.g. <code>{\"scan\": 19}</code>).</p> <p>Vendor native ids are space-separated <code>key=value</code> tokens, e.g. Thermo <code>\"controllerType=0 controllerNumber=1 scan=19\"</code> or Bruker <code>\"frame=1016 scan=1\"</code>. Integer components are returned as ints, so <code>spectrum.id_dict[\"scan\"]</code> gives the scan number without manual parsing.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.spot_id","title":"spot_id  <code>property</code>","text":"<pre><code>spot_id: str | None\n</code></pre> <p>Get spectrum spot id, or None if not present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.index","title":"index  <code>property</code>","text":"<pre><code>index: int | None\n</code></pre> <p>Get spectrum index, or None if not present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.default_array_length","title":"default_array_length  <code>property</code>","text":"<pre><code>default_array_length: int | None\n</code></pre> <p>Get spectrum default array length, or None if not present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.data_processing_ref","title":"data_processing_ref  <code>property</code>","text":"<pre><code>data_processing_ref: str | None\n</code></pre> <p>Get spectrum data processing reference, or None if not present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.source_file_ref","title":"source_file_ref  <code>property</code>","text":"<pre><code>source_file_ref: str | None\n</code></pre> <p>Get spectrum source file reference, or None if not present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.mz","title":"mz  <code>property</code>","text":"<pre><code>mz: np.ndarray | None\n</code></pre> <p>Get m/z array as a numpy array, or None if not present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.intensity","title":"intensity  <code>property</code>","text":"<pre><code>intensity: np.ndarray | None\n</code></pre> <p>Get intensity array as a numpy array, or None if not present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.charge","title":"charge  <code>property</code>","text":"<pre><code>charge: np.ndarray | None\n</code></pre> <p>Return the per-point charge array, or None if no charge binary array is present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.has_im","title":"has_im  <code>property</code>","text":"<pre><code>has_im: bool\n</code></pre> <p>Return True if this spectrum carries ion mobility data \u2014 either as a binary array (e.g. combined-IM frames) or as a scan-level cvParam (e.g. Bruker timsTOF PASEF MS2).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.im_types","title":"im_types  <code>property</code>","text":"<pre><code>im_types: set[BinaryDataArrayAccession]\n</code></pre> <p>Return the set of ion mobility array accessions present in this spectrum; empty set if none.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.spectrum_type","title":"spectrum_type  <code>cached</code> <code>property</code>","text":"<pre><code>spectrum_type: Literal['centroid', 'profile'] | None\n</code></pre> <p>Get spectrum type (centroid / profile / unknown).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.polarity","title":"polarity  <code>cached</code> <code>property</code>","text":"<pre><code>polarity: Literal['positive', 'negative'] | None\n</code></pre> <p>Get polarity (positive / negative / or unknown scan).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.TIC","title":"TIC  <code>cached</code> <code>property</code>","text":"<pre><code>TIC: float | None\n</code></pre> <p>Get total ion current (TIC) for this spectrum.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.ms_level","title":"ms_level  <code>cached</code> <code>property</code>","text":"<pre><code>ms_level: int | None\n</code></pre> <p>Get MS level for this spectrum.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.base_peak_mz","title":"base_peak_mz  <code>property</code>","text":"<pre><code>base_peak_mz: float | None\n</code></pre> <p>Base peak m/z for this spectrum (MS:1000504).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.base_peak_intensity","title":"base_peak_intensity  <code>property</code>","text":"<pre><code>base_peak_intensity: float | None\n</code></pre> <p>Base peak intensity for this spectrum (MS:1000505).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.lowest_observed_mz","title":"lowest_observed_mz  <code>property</code>","text":"<pre><code>lowest_observed_mz: float | None\n</code></pre> <p>Lowest observed m/z for this spectrum (MS:1000528).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.highest_observed_mz","title":"highest_observed_mz  <code>property</code>","text":"<pre><code>highest_observed_mz: float | None\n</code></pre> <p>Highest observed m/z for this spectrum (MS:1000527).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.has_products","title":"has_products  <code>property</code>","text":"<pre><code>has_products: bool\n</code></pre> <p>Check if this spectrum has a product list.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.products","title":"products  <code>property</code>","text":"<pre><code>products: list[Product]\n</code></pre> <p>Get a list of Product objects for the product list of this spectrum, or None</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.has_precursors","title":"has_precursors  <code>property</code>","text":"<pre><code>has_precursors: bool\n</code></pre> <p>Check if this spectrum has a precursor list.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.precursors","title":"precursors  <code>property</code>","text":"<pre><code>precursors: list[Precursor]\n</code></pre> <p>Get a list of Precursor objects for the precursor list of this spectrum, or None .</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.spectra_combination","title":"spectra_combination  <code>property</code>","text":"<pre><code>spectra_combination: (\n    Literal[\"no_combination\", \"median\", \"sum\", \"mean\"]\n    | None\n)\n</code></pre> <p>Get spectrum combination type (if any) for this spectrum.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.scans","title":"scans  <code>property</code>","text":"<pre><code>scans: list[Scan]\n</code></pre> <p>Get a list of Scan objects for the scan list of this spectrum, or None if no scan list is present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.is_single_scan","title":"is_single_scan  <code>property</code>","text":"<pre><code>is_single_scan: bool\n</code></pre> <p>Check if this spectrum has a single scan.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.lower_mz","title":"lower_mz  <code>property</code>","text":"<pre><code>lower_mz: float | None\n</code></pre> <p>Get scan window lower limit for this spectrum, if it has a single scan with a single scan window.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.upper_mz","title":"upper_mz  <code>property</code>","text":"<pre><code>upper_mz: float | None\n</code></pre> <p>Get scan window upper limit for this spectrum, if it has a single scan with a single scan window.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.scan_start_time","title":"scan_start_time  <code>property</code>","text":"<pre><code>scan_start_time: timedelta | None\n</code></pre> <p>Get scan start time for this spectrum, if it has a single scan.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.ion_injection_time","title":"ion_injection_time  <code>property</code>","text":"<pre><code>ion_injection_time: timedelta | None\n</code></pre> <p>Get ion injection time for this spectrum, if it has a single scan.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.ion_mobility","title":"ion_mobility  <code>property</code>","text":"<pre><code>ion_mobility: float | None\n</code></pre> <p>Scan-level ion mobility for this spectrum: inverse reduced ion mobility (preferred) or drift time. Common for Bruker timsTOF PASEF MS2, where mobility is a scan cvParam rather than a binary array. Returns None if the spectrum has no single scan or no mobility term.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.filter_string","title":"filter_string  <code>property</code>","text":"<pre><code>filter_string: str | None\n</code></pre> <p>Scan filter string for this spectrum (from its single scan), if present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.binary_arrays","title":"binary_arrays  <code>property</code>","text":"<pre><code>binary_arrays: list[BinaryDataArray]\n</code></pre> <p>Get a list of BinaryDataConverter objects for each binary data array.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.get_binary_array","title":"get_binary_array","text":"<pre><code>get_binary_array(id: str) -&gt; BinaryDataArray | None\n</code></pre> <p>Get a BinaryDataConverter object for the binary data array with the specified id.</p> Source code in <code>src/mzmlpy/spectra.py</code> <pre><code>def get_binary_array(self, id: str) -&gt; BinaryDataArray | None:\n    \"\"\"Get a BinaryDataConverter object for the binary data array with the specified id.\"\"\"\n    if self._binary_array_list is not None:\n        return self._binary_array_list.get_binary_array(id)\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.has_binary_array","title":"has_binary_array","text":"<pre><code>has_binary_array(id: str) -&gt; bool\n</code></pre> <p>Check if a binary data array with the specified id exists.</p> Source code in <code>src/mzmlpy/spectra.py</code> <pre><code>def has_binary_array(self, id: str) -&gt; bool:\n    \"\"\"Check if a binary data array with the specified id exists.\"\"\"\n    if self._binary_array_list is not None:\n        return self._binary_array_list.has_binary_array(id)\n    return False\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Spectrum.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/spectrum/#scan","title":"Scan","text":""},{"location":"api/spectrum/#mzmlpy.spectra.Scan","title":"mzmlpy.spectra.Scan  <code>dataclass</code>","text":"<pre><code>Scan(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A single scan event with timing, window, and CV parameter metadata.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.scan_windows","title":"scan_windows  <code>property</code>","text":"<pre><code>scan_windows: list[ScanWindow]\n</code></pre> <p>Get a list of ScanWindow objects for the scan window list of this scan.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.is_single_windowed_scan","title":"is_single_windowed_scan  <code>property</code>","text":"<pre><code>is_single_windowed_scan: bool\n</code></pre> <p>Check if this scan has a single scan window.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.lower_mz","title":"lower_mz  <code>property</code>","text":"<pre><code>lower_mz: float | None\n</code></pre> <p>Get scan window lower limit for this scan, if it has a single scan window.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.upper_mz","title":"upper_mz  <code>property</code>","text":"<pre><code>upper_mz: float | None\n</code></pre> <p>Get scan window upper limit for this scan, if it has a single scan window.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.scan_start_time","title":"scan_start_time  <code>property</code>","text":"<pre><code>scan_start_time: timedelta | None\n</code></pre> <p>Get scan start time for this scan.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.ion_injection_time","title":"ion_injection_time  <code>property</code>","text":"<pre><code>ion_injection_time: timedelta | None\n</code></pre> <p>Get ion injection time for this scan.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.inverse_reduced_ion_mobility","title":"inverse_reduced_ion_mobility  <code>property</code>","text":"<pre><code>inverse_reduced_ion_mobility: float | None\n</code></pre> <p>Inverse reduced ion mobility (1/K0) for this scan (MS:1002815), e.g. Bruker timsTOF.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.ion_mobility_drift_time","title":"ion_mobility_drift_time  <code>property</code>","text":"<pre><code>ion_mobility_drift_time: float | None\n</code></pre> <p>Ion mobility drift time for this scan (MS:1002476).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.filter_string","title":"filter_string  <code>property</code>","text":"<pre><code>filter_string: str | None\n</code></pre> <p>Instrument filter string for this scan (MS:1000512), e.g. a Thermo scan filter.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.faims_compensation_voltage","title":"faims_compensation_voltage  <code>property</code>","text":"<pre><code>faims_compensation_voltage: float | None\n</code></pre> <p>FAIMS compensation voltage for this scan (MS:1001581).</p> <p>Front-end high-field asymmetric waveform ion mobility (FAIMS) filtering: each scan may carry a single compensation voltage. See the PSI IM-MS/DIA recommendation v1.0, \u00a73.6.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.selexion_separation_voltage","title":"selexion_separation_voltage  <code>property</code>","text":"<pre><code>selexion_separation_voltage: float | None\n</code></pre> <p>SCIEX SelexION differential-mobility separation voltage for this scan (MS:1003394).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.selexion_compensation_voltage","title":"selexion_compensation_voltage  <code>property</code>","text":"<pre><code>selexion_compensation_voltage: float | None\n</code></pre> <p>SCIEX SelexION differential-mobility compensation voltage for this scan (MS:1003371).</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.Scan.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/spectrum/#scanwindow","title":"ScanWindow","text":""},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow","title":"mzmlpy.spectra.ScanWindow  <code>dataclass</code>","text":"<pre><code>ScanWindow(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>A scan window defining the m/z range acquired in a single scan.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.lower_mz","title":"lower_mz  <code>property</code>","text":"<pre><code>lower_mz: float | None\n</code></pre> <p>Get scan window lower limit for this spectrum.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.upper_mz","title":"upper_mz  <code>property</code>","text":"<pre><code>upper_mz: float | None\n</code></pre> <p>Get scan window upper limit for this spectrum.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.ScanWindow.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/spectrum/#binarydataarray","title":"BinaryDataArray","text":""},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray","title":"mzmlpy.spectra.BinaryDataArray  <code>dataclass</code>","text":"<pre><code>BinaryDataArray(element: ElementTree.Element)\n</code></pre> <p>               Bases: <code>_ParamGroup</code></p> <p>Wraps a single <code>binaryDataArray</code> XML element, handling base64 decoding and decompression.</p> <p>Exposes compression type, numeric encoding, semantic array type, and a <code>data</code> property that decodes the raw bytes into a NumPy array on each access.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.compression","title":"compression  <code>cached</code> <code>property</code>","text":"<pre><code>compression: CompressionTypeAccessions | None\n</code></pre> <p>Return the compression accession for this array, or None if no compression CV term is present.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.encoding","title":"encoding  <code>cached</code> <code>property</code>","text":"<pre><code>encoding: BinaryDataTypeAccession | None\n</code></pre> <p>Return the binary data type accession (e.g. 32-bit or 64-bit float/int), or None if absent.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.binary_array_type","title":"binary_array_type  <code>cached</code> <code>property</code>","text":"<pre><code>binary_array_type: BinaryDataArrayAccession | None\n</code></pre> <p>Return the semantic array type accession (e.g. m/z, intensity, ion mobility), or None if absent.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.data","title":"data  <code>property</code>","text":"<pre><code>data: np.ndarray\n</code></pre> <p>Decode and return the binary data as a NumPy array.</p> <p>Raw numeric encodings retain their declared dtype, including empty arrays. Numpress returns reconstructed float64 values. Arrays are writable. Decoding runs on every access. Store the result locally when using it repeatedly.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.ns","title":"ns  <code>cached</code> <code>property</code>","text":"<pre><code>ns: str\n</code></pre> <p>Get XML namespace from the element tag.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.cv_params","title":"cv_params  <code>cached</code> <code>property</code>","text":"<pre><code>cv_params: list[CvParam]\n</code></pre> <p>Parse cvParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.accessions","title":"accessions  <code>cached</code> <code>property</code>","text":"<pre><code>accessions: set[str]\n</code></pre> <p>Get a set of all accession numbers from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.names","title":"names  <code>cached</code> <code>property</code>","text":"<pre><code>names: set[str]\n</code></pre> <p>Get a set of all names from the cvParams.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.user_params","title":"user_params  <code>property</code>","text":"<pre><code>user_params: list[UserParam]\n</code></pre> <p>Parse userParams from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.ref_params","title":"ref_params  <code>property</code>","text":"<pre><code>ref_params: list[ReferenceableParamGroupRef]\n</code></pre> <p>Get a list of all referenceable parameters from the XML element.</p>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.serialize","title":"serialize","text":"<pre><code>serialize() -&gt; dict\n</code></pre> <p>return the full element content</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def serialize(self) -&gt; dict:\n    \"\"\"return the full element content\"\"\"\n    return {\n        \"tag\": self.element.tag,\n        \"attributes\": self.element.attrib,\n        \"text\": self.element.text,\n        \"children\": [_DataTreeWrapper(child).serialize() for child in self.element],\n    }\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.get_attribute","title":"get_attribute","text":"<pre><code>get_attribute(attr_name: str) -&gt; str | None\n</code></pre> <p>Get an attribute value from the element.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_attribute(self, attr_name: str) -&gt; str | None:\n    \"\"\"Get an attribute value from the element.\"\"\"\n    return self.element.attrib.get(attr_name)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.get_cvparm","title":"get_cvparm","text":"<pre><code>get_cvparm(id: str) -&gt; CvParam | None\n</code></pre> <p>Get a cvParam by accession or name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_cvparm(self, id: str) -&gt; CvParam | None:\n    \"\"\"Get a cvParam by accession or name.\"\"\"\n    for cv_param in self.cv_params:\n        if cv_param.accession == id or cv_param.name == id:\n            return cv_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.has_cvparm","title":"has_cvparm","text":"<pre><code>has_cvparm(id: str) -&gt; bool\n</code></pre> <p>Check if a cvParam with the given accession or name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_cvparm(self, id: str) -&gt; bool:\n    \"\"\"Check if a cvParam with the given accession or name exists.\"\"\"\n    return any(cv_param.accession == id or cv_param.name == id for cv_param in self.cv_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.cv_float","title":"cv_float","text":"<pre><code>cv_float(id: str) -&gt; float | None\n</code></pre> <p>Return a cvParam's value as a float, or None if the term is absent or has no value.</p> <p>Raises ValueError naming the term and its bad value if the value is present but not numeric \u2014 more actionable than a bare \"could not convert string to float\".</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_float(self, id: str) -&gt; float | None:\n    \"\"\"Return a cvParam's value as a float, or None if the term is absent or has no value.\n\n    Raises ValueError naming the term and its bad value if the value is present but not\n    numeric \u2014 more actionable than a bare \"could not convert string to float\".\n    \"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return float(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-numeric value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.cv_int","title":"cv_int","text":"<pre><code>cv_int(id: str) -&gt; int | None\n</code></pre> <p>Return a cvParam's value as an int, or None if absent or valueless (see :meth:<code>cv_float</code>).</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def cv_int(self, id: str) -&gt; int | None:\n    \"\"\"Return a cvParam's value as an int, or None if absent or valueless (see :meth:`cv_float`).\"\"\"\n    cv_param = self.get_cvparm(id)\n    if cv_param is None or cv_param.value is None:\n        return None\n    try:\n        return int(cv_param.value)\n    except ValueError as e:\n        raise ValueError(\n            f\"CV param {cv_param.name or id!r} ({id}) has a non-integer value {cv_param.value!r}\"\n        ) from e\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.get_user_param","title":"get_user_param","text":"<pre><code>get_user_param(name: str) -&gt; UserParam | None\n</code></pre> <p>Get a userParam by name.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_user_param(self, name: str) -&gt; UserParam | None:\n    \"\"\"Get a userParam by name.\"\"\"\n    for user_param in self.user_params:\n        if user_param.name == name:\n            return user_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.has_user_param","title":"has_user_param","text":"<pre><code>has_user_param(name: str) -&gt; bool\n</code></pre> <p>Check if a userParam with the given name exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_user_param(self, name: str) -&gt; bool:\n    \"\"\"Check if a userParam with the given name exists.\"\"\"\n    return any(user_param.name == name for user_param in self.user_params)\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.get_ref_param","title":"get_ref_param","text":"<pre><code>get_ref_param(\n    ref: str,\n) -&gt; ReferenceableParamGroupRef | None\n</code></pre> <p>Get a referenceable parameter by ref.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def get_ref_param(self, ref: str) -&gt; ReferenceableParamGroupRef | None:\n    \"\"\"Get a referenceable parameter by ref.\"\"\"\n    for ref_param in self.ref_params:\n        if ref_param.ref == ref:\n            return ref_param\n    return None\n</code></pre>"},{"location":"api/spectrum/#mzmlpy.spectra.BinaryDataArray.has_ref_param","title":"has_ref_param","text":"<pre><code>has_ref_param(ref: str) -&gt; bool\n</code></pre> <p>Check if a referenceable parameter with the given ref exists.</p> Source code in <code>src/mzmlpy/elems/dtree_wrapper.py</code> <pre><code>def has_ref_param(self, ref: str) -&gt; bool:\n    \"\"\"Check if a referenceable parameter with the given ref exists.\"\"\"\n    return any(ref_param.ref == ref for ref_param in self.ref_params)\n</code></pre>"},{"location":"api/validation/","title":"Validation and filtering","text":""},{"location":"api/validation/#mzmlpy.validate","title":"mzmlpy.validate","text":"<pre><code>validate(\n    file: str | Path,\n    *,\n    decode_binary: bool = False,\n    check_index: bool = False\n) -&gt; ValidationReport\n</code></pre> <p>Validate plain or gzip mzML without creating or repairing caches.</p> <p>The default checks XML structure, list counts, IDs, references, index ID agreement, and supported array metadata. Set decode_binary to decode every array and compare lengths. Set check_index to seek to XML footer offsets and verify their targets. This does not perform full XSD, controlled-vocabulary, or embedded gzip index validation. Memory grows with IDs, references, and findings, plus the largest record.</p> <p>File-open errors raise OSError. Malformed content is returned as report issues.</p> Source code in <code>src/mzmlpy/validation.py</code> <pre><code>def validate(file: str | Path, *, decode_binary: bool = False, check_index: bool = False) -&gt; ValidationReport:\n    \"\"\"Validate plain or gzip mzML without creating or repairing caches.\n\n    The default checks XML structure, list counts, IDs, references, index ID agreement,\n    and supported array metadata. Set decode_binary to decode every array and compare\n    lengths. Set check_index to seek to XML footer offsets and verify their targets.\n    This does not perform full XSD, controlled-vocabulary, or embedded gzip index validation.\n    Memory grows with IDs, references, and findings, plus the largest record.\n\n    File-open errors raise OSError. Malformed content is returned as report issues.\n    \"\"\"\n    with open(file, \"rb\") as raw:\n        compressed = raw.read(2) == b\"\\x1f\\x8b\"\n        raw.seek(0)\n        if compressed:\n            with gzip.GzipFile(fileobj=raw) as handle:\n                return _validate_stream(cast(BinaryIO, handle), decode_binary=decode_binary, check_index=check_index)\n        return _validate_stream(raw, decode_binary=decode_binary, check_index=check_index)\n</code></pre>"},{"location":"api/validation/#mzmlpy.ValidationReport","title":"mzmlpy.ValidationReport  <code>dataclass</code>","text":"<pre><code>ValidationReport(\n    issues: tuple[ValidationIssue, ...],\n    spectrum_count: int,\n    chromatogram_count: int,\n    arrays_decoded: int,\n    index_entries_checked: int,\n    complete: bool,\n    decode_binary: bool,\n    check_index: bool,\n)\n</code></pre> <p>Results of the requested checks, without claiming full schema or ontology validation.</p> <p>Structural validation scans XML and records IDs and references without decoding arrays. Binary decoding and byte-offset verification are explicit, potentially expensive options.</p>"},{"location":"api/validation/#mzmlpy.ValidationReport.valid","title":"valid  <code>property</code>","text":"<pre><code>valid: bool\n</code></pre> <p>Whether parsing completed and the requested checks found no errors.</p>"},{"location":"api/validation/#mzmlpy.ValidationReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre> <p>Return JSON-serializable results, including the computed valid flag.</p> Source code in <code>src/mzmlpy/validation.py</code> <pre><code>def to_dict(self) -&gt; dict:\n    \"\"\"Return JSON-serializable results, including the computed valid flag.\"\"\"\n    return {\"valid\": self.valid, **asdict(self)}\n</code></pre>"},{"location":"api/validation/#mzmlpy.ValidationIssue","title":"mzmlpy.ValidationIssue  <code>dataclass</code>","text":"<pre><code>ValidationIssue(\n    code: str,\n    message: str,\n    location: str,\n    severity: Literal[\"error\", \"warning\"] = \"error\",\n)\n</code></pre> <p>One machine-readable finding with a stable code and an XML location.</p>"},{"location":"api/validation/#mzmlpy.SpectrumFilter","title":"mzmlpy.SpectrumFilter  <code>dataclass</code>","text":"<pre><code>SpectrumFilter(\n    ms_level: int | None = None,\n    retention_time: (\n        tuple[float | None, float | None] | None\n    ) = None,\n    polarity: Literal[\"positive\", \"negative\"] | None = None,\n    precursor_mz: (\n        tuple[float | None, float | None] | None\n    ) = None,\n    spectrum_type: (\n        Literal[\"centroid\", \"profile\"] | None\n    ) = None,\n    mobility_type: (\n        Literal[\"inverse_reduced\", \"drift_time\"] | None\n    ) = None,\n    ion_mobility: (\n        tuple[float | None, float | None] | None\n    ) = None,\n    faims_voltage: (\n        tuple[float | None, float | None] | None\n    ) = None,\n)\n</code></pre> <p>Combine metadata criteria with AND, without decoding binary arrays.</p> <p>Retention times are inclusive bounds in seconds, matched against any scan. Precursor m/z bounds overlap any reported isolation window, or match a selected ion when that precursor has no usable isolation window. Missing metadata does not match a requested criterion. Either range endpoint may be None for an open bound.</p>"},{"location":"api/validation/#mzmlpy.SpectrumFilter.matches","title":"matches","text":"<pre><code>matches(spectrum: Spectrum) -&gt; bool\n</code></pre> <p>Return whether a spectrum satisfies every supplied criterion.</p> Source code in <code>src/mzmlpy/filtering.py</code> <pre><code>def matches(self, spectrum: Spectrum) -&gt; bool:\n    \"\"\"Return whether a spectrum satisfies every supplied criterion.\"\"\"\n    if self.spectrum_type is not None and spectrum.spectrum_type != self.spectrum_type:\n        return False\n    if self.mobility_type is not None:\n        attribute = (\n            \"inverse_reduced_ion_mobility\" if self.mobility_type == \"inverse_reduced\" else \"ion_mobility_drift_time\"\n        )\n        if not any(_within(getattr(scan, attribute), self.ion_mobility or (None, None)) for scan in spectrum.scans):\n            return False\n    if self.faims_voltage is not None:\n        if not any(_within(scan.faims_compensation_voltage, self.faims_voltage) for scan in spectrum.scans):\n            return False\n    if self.ms_level is not None and spectrum.ms_level != self.ms_level:\n        return False\n    if self.polarity is not None and spectrum.polarity != self.polarity:\n        return False\n    if self.retention_time is not None:\n        if not any(\n            (time := scan.scan_start_time) is not None and _within(time.total_seconds(), self.retention_time)\n            for scan in spectrum.scans\n        ):\n            return False\n    if self.precursor_mz is not None and not self._matches_precursor(spectrum):\n        return False\n    return True\n</code></pre>"}]}